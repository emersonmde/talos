# Phase 10 Pi 5 Local Cd Fixed Dirs Entry Delta Proof

Task: phase10-pi5-local-cd-fixed-dirs-entry-delta-proof-20260602
Status: accepted
Commit: recorded in talos-supervisor-state.json after the accepted proof
commit is created.

## Goal

Run the entry-delta fixed cd candidate on serialized Pi 5 hardware and either
prove the full user-visible cd fixed-directories feature or retain the next
earliest boundary with complete evidence.

## Candidate

The selected archive was the accepted entry-delta fix-core artifact:

- archive: target/talos-rpi5-local-cd-fixed-dirs-entry-delta-fix-core.tar.gz
- archive sha256:
  52eb5d54aef19044ae9af0689786f49bc573b46bbff0d71956c3fbe6b45011a3
- kernel sha256:
  c9b174c3fe087ac6c887c102c9b2a8fe143ea265027d3bab498eb8c581e7464f
- kernel size: 109800 bytes

Archive review passed before publication. Static string inspection retained
rpi5-local-cd-fixed-dirs-proof, pi5-local-cd-fixed-dirs-complete, and
TALOS: command loop proof entered while omitting TALOS: asm_start and
TALOS: asm_pre_rust_entry.

## Run

Accepted evidence is retained under:

tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-entry-delta-proof/local1-entry-delta-candidate/.

The worker held hardwareTestLock for the serialized proof, captured fresh
serial cursor 4019998 and TFTP cursor 4069323 before the fixed-port power
cycle, published only the selected entry-delta cd archive, then restored the
named pre-run boot snapshot before releasing the lock. Pre-run and post-restore
boot tree hashes both matched:

a0452458391d0e398b7e17e0f068bb652235f666bf277d004e0e214626128d10.

The settled same-cursor TFTP loop returned 13 events on attempt 6 before
restore. The retained TFTP delta includes two fresh
da591740/kernel_2712.img serves of the expected 109800-byte candidate kernel.

After the candidate reached the command-loop proof and printed
rpi5-local-cd-fixed-dirs-proof: ready command=0, the worker sent the original
nine-command feature sequence over serial:

- pwd
- cd /etc
- pwd
- cd /bin
- pwd
- cd /
- pwd
- cd /missing
- pwd

The retained serial transcript shows cwd outputs for /, /etc, /bin, and /
again after returning to root; it records cd /missing as
status=unexpected-argument with the following pwd still reporting /; it then
prints ready-for-next prompt=true, pi5-local-cd-fixed-dirs-complete, and
rpi5-local-cd-fixed-dirs-proof: PASS.

## Classification

Final classification: cd-feature-pass-retained.

This accepts the original bounded cd fixed-directories Pi 5 hardware proof via
the entry-delta candidate. The accepted feature remains kernel-backed and
bounded to fixed directories /, /etc, and /bin. It does not accept broad POSIX
chdir semantics, relative paths, path traversal, descriptor-backed filesystem
syscalls, userspace shell execution, process lifecycle, terminal/session
behavior, networking, SSH, RP1/PCIe, UART interrupt ownership, DMA, or cache
driver policy.

## Evidence

- candidate identity, archive review, hashes, lab status, snapshot, publish,
  power-cycle, serial write/observe artifacts, TFTP logs, restore, and
  classification:
  tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-entry-delta-proof/local1-entry-delta-candidate/.
- proof result:
  tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-entry-delta-proof/local1-entry-delta-candidate/proof-result.txt.
- classification summary:
  tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-entry-delta-proof/local1-entry-delta-candidate/classification-summary.json.
- serial transcript:
  tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-entry-delta-proof/local1-entry-delta-candidate/serial-transcript.txt.
- full post-restore serial observe from the original pre-run cursor:
  tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-entry-delta-proof/local1-entry-delta-candidate/serial-transcript-full-post-restore.txt.
- settled TFTP delta:
  tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-entry-delta-proof/local1-entry-delta-candidate/tftp-delta-settled-before-restore.json.
- restore proof:
  tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-entry-delta-proof/local1-entry-delta-candidate/restore-proof.txt.

## Validation

- pre-run static state: git status was clean except supervisor state outside the
  Talos repo and evidence generated by this task.
- image/archive inspection: scripts/rpi5-archive-review.sh passed for the
  selected entry-delta cd candidate archive.
- static image/string inspection: the selected candidate image retained the cd
  proof strings and TALOS: command loop proof entered with no raw
  TALOS: asm_start or TALOS: asm_pre_rust_entry marker strings.
- lab-controller API: health, status, boot files, named snapshot, publish,
  fixed-port power-cycle, serial write/observe/peek, TFTP logs, named snapshot
  restore, and post-restore status artifacts are retained.
- TFTP/lab-controller API: settled same-cursor delta before restore retained
  two 109800-byte da591740/kernel_2712.img serves for the selected candidate
  archive.
- serial hardware boot/output: retained fresh serial output showed command-loop
  entry, the original pwd/cd sequence, expected cwd outputs, rejected missing
  directory with cwd unchanged, ready-for-next prompt, final classification,
  and PASS.
- restore proof: post-restore status returned the lab to the pre-run tree hash,
  and hardwareTestLock was released/restored after the run.
- static inspection: git diff --check passed after evidence/doc updates.
- docs validation: mdbook build passed after roadmap update.
- staged static inspection: git diff --cached --check passed before commit.

## Next Action

The cd closeout checkpoint is mechanically unblocked on the next worker wake if
supervisorIntervention remains inactive and hardwareTestLock remains
unlocked/restored.
