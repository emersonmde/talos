# Phase 10 Pi 5 Local Cd Fixed Dirs Kernel Entry Provenance Proof

Task: phase10-pi5-local-cd-fixed-dirs-kernel-entry-provenance-proof-20260602
Status: accepted-blocked-firmware-fetch-no-kernel-entry-marker

## Goal

Use the accepted cd fixed-directories kernel-entry provenance core on the
serialized Raspberry Pi 5 path exactly far enough to classify whether the
candidate reaches the assembly entry markers, Rust entry, DTB scan, or the full
cd feature.

## Run

Evidence root:
tasks/evidence/2026-06-02-pi5-local-cd-fixed-dirs-kernel-entry-provenance-proof/local1-entry-provenance-candidate/.

The worker held hardwareTestLock from 2026-06-02T16:18:31.492Z to
2026-06-02T16:19:03.247Z, restored the named pre-run boot snapshot, and
released the lock with hardwareTestLock.restored=true.

Pre-run and post-restore boot tree hash:
`a0452458391d0e398b7e17e0f068bb652235f666bf277d004e0e214626128d10`.
Published candidate tree hash:
`1c1a5d6bc0801d69f0ba7368a81460937eae76302c1a5c114d004e9bf4a1f7e7`.

Candidate identity:

- Archive:
  `target/talos-rpi5-local-cd-fixed-dirs-entry-provenance-core.tar.gz`.
- Archive sha256:
  `80607b260c0f955d9e4184a72eb2dbf0d7e2347aab2320d241bd3c486c0252c5`.
- Kernel sha256:
  `dd9fa1e0fc75b0f717099f9d027b95d3416afb948a72c180a591c19638c8a780`.
- Archive review passed with `kernel_2712.img` size 110008 bytes.
- Archive string inspection retained `TALOS: asm_start`,
  `TALOS: asm_pre_rust_entry`, `rpi5-local-cd-fixed-dirs-proof`, and
  `pi5-local-cd-fixed-dirs-complete`.

TFTP evidence was collected before restore from the same pre-run cursor. The
settled delta reached a stable cursor after 15 attempts and retained 13 events,
including two fresh `kernel_2712.img` serves of 110008 bytes each. Serial was
observed only after the settled TFTP window. The fresh serial artifact retained
two bytes, NUL and space, and did not contain `TALOS: asm_start`,
`TALOS: asm_pre_rust_entry`, `TALOS: rust_entry`,
`TALOS: dtb memory scan start`, a prompt, cd transcript, final classification,
or `rpi5-local-cd-fixed-dirs-proof: PASS`.

The full pwd/cd transcript was not typed because no fresh prompt or command-loop
ready marker appeared.

## Classification

Final classification: firmware-fetch-no-kernel-entry-marker.

This is narrower than the prior DTB-scan blocker: firmware fetched the accepted
entry-provenance candidate kernel from TFTP, but no earliest assembly marker was
retained afterward. The original Pi 5 cd fixed-directories feature proof remains
blocked. This task does not accept the cd feature, because it lacks the required
pwd/cd sequence, cwd outputs, rejected nonexistent directory with cwd unchanged,
ready prompt, `pi5-local-cd-fixed-dirs-complete`, and
`rpi5-local-cd-fixed-dirs-proof: PASS`.

## Validation

- pre-run static state: Talos git status was clean except task evidence
  generated by this task.
- image/archive inspection: `scripts/rpi5-archive-review.sh` passed for the
  candidate archive, and string inspection confirmed the entry-provenance
  markers were present in the archive kernel image.
- lab-controller API: health, status, boot files, snapshot, publish, fixed-port
  power-cycle, TFTP logs, serial observe/peek, named snapshot restore, and
  post-restore status artifacts are retained.
- TFTP/lab-controller API: settled same-cursor delta before restore retained 13
  events, including two 110008-byte `kernel_2712.img` serves.
- serial hardware boot/output: post-TFTP serial observe from the captured
  cursor retained only NUL/space and no entry marker, Rust marker, DTB scan,
  prompt, cd transcript, classification, or PASS.
- restore proof: post-restore status returned the lab to the pre-run tree hash,
  and hardwareTestLock was released/restored after the run.

## Next Action

The original cd proof remains blocked with the narrowed hardware classification.
Supervisor planning is required for any follow-up implementation task before
another Pi 5 rerun; do not promote the cd closeout checkpoint.
