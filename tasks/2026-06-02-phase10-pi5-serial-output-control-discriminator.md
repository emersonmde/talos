# Phase 10 Pi 5 Serial Output Control Discriminator

Task: phase10-pi5-serial-output-control-discriminator-20260602
Status: accepted-prompt-capture-healthy

## Goal

Determine whether the Pi 5 lab can currently retain prompt/PASS serial output
for an already accepted prompt-capable Talos local-serial control before any
more cd fixed-directories candidate proof runs.

## Scope

This task ran exactly one serialized accepted-control discriminator. It used the
accepted literal-echo control archive:

- selected archive: `target/talos-rpi5-local-literal-echo-local3.tar.gz`
- selected control source:
  `phase10-pi5-serial-write-ingress-control-proof-20260601` and
  `phase10-pi5-accepted-control-settled-tftp-preentry-discriminator-20260602`
- selected control commit:
  `0aceae6708ac57b027184c436603bc24308e5d92`
- archive sha256:
  `7cc63a02e2d5dc68abd2127bad0867bc0c1bd0830f56c7ba74a6efbfe64438f5`
- kernel sha256:
  `63d16359baa5e3c8631f582014a3bcd14f78b64023717bb1546dcc9e9e4c3826`

No Talos runtime code, proof harness code, cd candidate archive, command-loop
behavior, wait policy, marker names, lab-controller code, roadmap, or ADR was
changed. The cd fixed-directories candidate was not published or rerun.

## Hardware Run

Evidence root:
`tasks/evidence/2026-06-02-pi5-serial-output-control-discriminator/local1-literal-echo-control-two-mode/`.

The run acquired the durable hardware lock at 2026-06-02T11:22:05Z and
released it at 2026-06-02T11:23:28Z. Pre-run and post-restore tree hashes both
matched:
`a0452458391d0e398b7e17e0f068bb652235f666bf277d004e0e214626128d10`.

Observation modes:

- cursor-based `serial/observe` from fresh cursor `4015186` retained fresh
  Raspberry Pi firmware output from the fixed-port power cycle.
- non-draining `serial/peek` retained the selected control Talos boot output,
  `talos>` prompt, ready-for-next prompt, accepted control classification
  `pi5-local-literal-echo-complete`, and
  `rpi5-local-literal-echo-proof: PASS`.

The minimal control write request was sent after the prompt was visible, but the
control had already emitted its accepted PASS path; no response text from the
write was needed for this discriminator because the retained prompt, next-prompt
readiness, accepted control classification, and PASS line prove serial-output
capture is healthy.

## TFTP Evidence

The task captured pre-run TFTP cursor `4053111` and queried the same cursor 12
times before restore. The TFTP API delta remained empty on each query. This is a
lab-log observation gap, not a Talos feature failure for this discriminator:
serial output retained current firmware network-boot messages and the selected
Talos accepted-control prompt/PASS output from the same staged archive.

The empty TFTP API delta is retained in:

- `tftp-cursor-start.json`
- `tftp-settle-loop-summary.txt`
- `tftp-delta-before-restore.json`

## Result

Classification: `pi5-serial-output-control-healthy`.

PASS line: `rpi5-serial-output-control-discriminator: PASS`.

This unblocks a future unchanged cd fixed-directories Pi 5 proof rerun if the
hardware lock remains unlocked/restored. The cd proof still must collect its own
candidate identity, serial transcript, settled TFTP evidence, restore proof, and
PASS/classification evidence.

## Validation

- pre-run static state: Talos git status was clean before hardware except for
  task evidence generated by this task after the run.
- image/archive inspection: `scripts/rpi5-archive-review.sh` passed for the
  selected accepted literal-echo archive.
- lab-controller API: health, pre/post status, snapshot, publish, boot files,
  fixed-port power-cycle, serial observe, serial peek, serial write, TFTP logs,
  and named restore artifacts are retained.
- serial hardware boot/output: non-draining serial peek retained the accepted
  control prompt/PASS and next-prompt readiness; cursor observe retained fresh
  firmware serial bytes from the same power cycle.
- restore proof: pre/post boot tree hashes matched and the hardware lock was
  released/restored.
- static inspection: `git diff --check` passed.
- documentation: mdBook was not required because mdBook docs were not touched.
- staged static inspection: `git diff --cached --check` passed before commit.
