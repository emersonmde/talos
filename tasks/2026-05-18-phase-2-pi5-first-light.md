# Phase 2 Pi 5 First Light

## Task

- Title: Pi 5 firmware handoff and firmware-preserved serial first light
- Owner: Seldon / supervisor loop
- Date: 2026-05-18
- Milestone: Phase 2.1
- Scope: Prepare, review, publish, and validate the first Talos boot archive for the physical Raspberry Pi 5 lab.

## Goal

Boot Talos through the normal Raspberry Pi 5 firmware path and capture enough serial evidence to prove that Talos code reached the CPU entry point.

## Acceptance Criteria

- A local boot tree can be staged with `config.txt`, `cmdline.txt`, `bcm2712-rpi-5-b.dtb`, and Talos `kernel_2712.img`.
- Pre-hardware review confirms the archive contents match the lab-controller contract and does not require direct UniFi access or credential handling.
- The lab publishes the archive through `PUT /boot/archive`.
- A single controlled power cycle reaches Talos code on the Pi 5.
- Serial output includes Talos version, exception level, DTB physical address, target name, service summary, and panic/halt behavior if boot fails later.
- A failed boot can be rolled back, and the evidence record captures the archive digest, publish result, power-cycle time, serial tail, classification, suspected cause, and next action.

Required validation level:

- Local preparation: formatting/lint/typecheck plus Pi 5 target build.
- Pre-hardware readiness: static inspection of staged archive contents.
- Acceptance: lab-controller API path plus serial hardware boot/output.

## Context

- Lab API: `http://talos-lab-api:8080`.
- Serial is configured and reachable through the lab API.
- The lab API currently lists a valid Pi 5 boot tree, including `bcm2712-rpi-5-b.dtb`, `kernel_2712.img`, `initramfs_2712`, and overlays.
- The supervisor must not publish archives or power-cycle the Pi without the hardware-test lock, acceptance criteria, and pre-hardware review.
- Talos has local staging helpers: `scripts/rpi5-image.sh` and `scripts/rpi5-boot-tree.sh`.

## Work Performed

- Added local Pi 5 image generation and boot-tree staging scripts.
- Documented the staging flow in the lab-controller notes.
- Marked the Phase 2.1 archive-publish criterion as having local staging support, with publish still intentionally not run.
- Split QEMU and Pi 5 console selection so the Pi 5 build no longer writes
  diagnostics to QEMU's PL011 address.
- Added an early Pi 5 firmware-preserved PL011 console path at physical
  `0x10_7d00_1000`, derived from Raspberry Pi Linux `bcm2712.dtsi`
  `soc@107c000000` ranges plus `uart10: serial@7d001000`.
- Re-routed the first-light Pi 5 console to RP1 UART0 because the lab cable is
  on the 40-pin header path.
- Corrected the RP1 UART0 physical address from the PCIe non-prefetchable CPU
  window `0x1f_0003_0000` to the firmware-preserved RP1 UART mapping
  `0x1c_0003_0000`. Raspberry Pi firmware reports `RP1_UART
  0000001c00030000` with `enable_rp1_uart=1`, and Linux describes RP1 UART0 as
  RP1 bus offset `0xc0_40030000`.

## Evidence

Completed local checks:

~~~bash
sh -n scripts/rpi5-image.sh scripts/rpi5-boot-tree.sh
./scripts/rpi5-image.sh
./scripts/rpi5-boot-tree.sh target/test-pi-boot-source target/test-rpi5-boot-tree
tar -C target/test-rpi5-boot-tree -czf target/test-talos-rpi5-boot.tar.gz .
./scripts/rpi5-archive-review.sh target/test-talos-rpi5-boot.tar.gz
cargo fmt --check
cargo -Zjson-target-spec test
cargo -Zjson-target-spec build --target targets/aarch64-talos-rpi5-bcm2712.json
mdbook build
~~~

Results:

- Shell syntax check passed.
- Pi 5 image generation produced `target/aarch64-talos-rpi5-bcm2712/debug/kernel_2712.img`.
- Synthetic boot-tree staging produced the required archive-root files: `config.txt`, `cmdline.txt`, `bcm2712-rpi-5-b.dtb`, and `kernel_2712.img`.
- Synthetic tar archive inspection showed only relative safe paths.
- QEMU no_std tests passed: 3 tests.
- Pi 5 target build passed.
- Pi 5 image generation passed after routing the Pi 5 console to UART10.
- mdBook build passed.
- 2026-05-19 supervisor recheck: Pi 5 target build passed.
- 2026-05-19 supervisor recheck: QEMU smoke passed and printed the expected Talos boot banner.
- 2026-05-19 lab API check: health ok, serial.configured=true, boot tree lists required Pi 5 files, and rollback is available.
- 2026-05-19 lab API check: serial peek returned the retained Linux login prompt, proving the serial read path is live.
- 2026-05-19 lab API check: UniFi guard status reported `GET /proxy/network/api/s/default/stat/sta/88:a2:9e:ae:c8:7f: HTTP Error 400`, so no power-cycle was attempted.
- 2026-05-19 Matthew clarified the Pi OS Lite TFTP tree is disposable and authorized replacing it with Talos boot files.
- 2026-05-19 official Raspberry Pi boot docs recheck: Pi 5 firmware defaults to `kernel_2712.img`, requires `config.txt` for a bootable partition, supports `enable_rp1_uart=1` to preserve 40-pin header RP1 UART0 at 115200, and `os_check=0` is appropriate for bare-metal development.
- 2026-05-19 local validation after RP1 UART0 routing passed: `cargo fmt --check`, `cargo -Zjson-target-spec test`, `cargo -Zjson-target-spec build --target targets/aarch64-talos-rpi5-bcm2712.json`, and `./scripts/rpi5-image.sh`.
- 2026-05-19 staged archive `target/talos-rpi5-boot.tar.gz` with digest `9db4ed077db8a5ae7f5985dfab750ff194c0c6eade2e78454753520b720cd644`; contents were exactly `config.txt`, `cmdline.txt`, `bcm2712-rpi-5-b.dtb`, `kernel_2712.img`, `overlays/overlay_map.dtb`, `overlays/bcm2712d0.dtbo`, and `overlays/uart0-pi5.dtbo`.
- 2026-05-19 lab publish succeeded through `PUT /boot/archive`; the API reported `ok=true`, `file_count=7`, `extracted_bytes=169919`, and rollback archive `/state/boot-previous.tar.gz`.
- 2026-05-19 the controlled power-cycle did not run. Five bounded status retries all reported the UniFi guard error `GET /proxy/network/api/s/default/stat/sta/88:a2:9e:ae:c8:7f: HTTP Error 400`, so the lab API failed closed before power action.
- 2026-05-19 serial cursor remained at 57 with the retained `talos-pi5 login:` prompt after the blocked power attempt, confirming no new boot output was observed.
- 2026-05-19 follow-up lab API check after the fixed-port API update: status returned `ok=true`, `serial.configured=true`, guard `fixed-port`, Weathertop port 8 `poe_state=UP`, and the active boot tree still contained the published Talos files.
- 2026-05-19 controlled power cycle succeeded through `POST /power/cycle`; response `ok=true`, guard `mode=fixed-port`, `switch_name=Weathertop`, `port_idx=8`, `poe_state=UP`.
- 2026-05-19 serial observe from cursor 57 captured RP1 firmware output only: `1.81 RP1 FW: load 0` and `1.82 RP1_BOOT chip ID: 0x20001927`, followed by a NUL/newline on the longer observe window. No Talos banner, boot-info line, or panic/halt output was observed.
- 2026-05-19 rollback succeeded through `POST /boot/rollback`; the restored Pi OS Lite boot tree contains 414 files including `config.txt`, `cmdline.txt`, `bcm2712-rpi-5-b.dtb`, `initramfs_2712`, `kernel8.img`, and `kernel_2712.img`.
- 2026-05-19 recovery power cycle succeeded after rollback. Serial confirmed Linux boot resumed: `Linux 6.12.75+rpt-rpi-2712`, RP1 PCIe enumeration, SD root mount, systemd start, hostname `talos`, and root filesystem remounted read/write.
- 2026-05-19 post-hardware local review found the likely load-address bug: the shared linker placed the Pi 5 image at `0x40200000`, which is QEMU virt's `0x40000000` RAM base plus the arm64 Image `0x200000` text offset. The Pi 5 firmware path should use the same text offset from the Pi RAM base, so the Pi 5 target now uses `linker-rpi5.ld` and links `_start` at `0x00200000`.
- 2026-05-19 validation after the Pi 5 linker split passed: `cargo fmt --check`, `cargo -Zjson-target-spec test`, `cargo -Zjson-target-spec build --target targets/aarch64-talos-rpi5-bcm2712.json`, `./scripts/rpi5-image.sh`, and `./scripts/qemu-smoke.sh`. Static symbol inspection confirmed Pi 5 `_start` and `__kernel_start` at `0x00200000`; QEMU smoke still printed the Talos boot banner and PASS line.
- 2026-05-19 follow-up hardware-loop note: a second publish/power-cycle attempt returned `ok=true`, but the serial cursor did not advance, so it did not produce valid Talos boot evidence. The boot tree was rolled back to Pi OS Lite and status again showed 414 boot files with `kernel8.img`, `kernel_2712.img`, `initramfs_2712`, `config.txt`, `cmdline.txt`, and `bcm2712-rpi-5-b.dtb`; retained Linux logs still show root remounted read/write.
- 2026-05-19 added a Pi-only assembly marker that writes `T0\r\n` to RP1 UART0 immediately after arm64 Image entry branches to the real start label, before preserving x0, clearing BSS, setting the stack, or entering Rust. This is intended to distinguish "firmware did not reach Talos entry" from "Rust or early memory setup failed before normal console output."
- 2026-05-19 Matthew pushed back on treating the missing serial cursor movement as a hard blocker. Follow-up review found a concrete marker bug: Talos used `0x1f00030000`, while firmware and Pi 5 bare-metal references point at `0x1c00030000` for the preserved RP1 UART0 mapping. Talos now uses `0x1c_0003_0000` in both the Pi 5 target map and the assembly marker, and staged `cmdline.txt` normalizes `earlycon=pl011,mmio32,0x1c00030000`.
- 2026-05-19 local validation after the RP1 UART address correction passed: `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 boot-tree generation, `./scripts/qemu-smoke.sh`, and `mdbook build`. QEMU smoke still printed the Talos boot banner and PASS line.
- 2026-05-19 hardware attempt with the corrected `0x1c00030000` marker published archive digest `a1063ed6fe9b32decd729389bc40c132826a35b16cd6b671e744f8cdfb479ed4`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `26367` to `27015` and emitted Pi firmware/DDR logs through `DDR 4267 1 0 64 152 BL:1`, but no `T0` marker or Talos banner appeared.
- 2026-05-19 changed the Pi assembly marker to initialize PL011 control/line-control using the existing firmware clock/divisor state, clear interrupts, bound the TX-full wait, and emit `T1\r\n` so an unexpected flag state cannot hang before Rust. Local validation passed again.
- 2026-05-19 hardware attempt with the bounded `T1` marker published archive digest `49ceeba809c0e0a88497c7265e2459e7e39d2a07fc74e6d27bb9dc794954d146`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `27017` to `27665` and again stopped after the same firmware/DDR log boundary with no `T1` marker.
- 2026-05-19 added a duplicate `kernel8.img` alongside `kernel_2712.img` because the lab API status helper reported `active_name=kernel8.img` even when the Talos config selected `kernel_2712.img`. Local validation passed. Hardware attempt published archive digest `5b2bea918f23756c64cbc98529b55d95e246c342cc364010ee5581281ff8fbe5`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `27665` to `28313` and still stopped at the firmware/DDR boundary without a Talos marker.
- 2026-05-19 non-hardware image-format review found the arm64 Image header advertised `image_size=0x200000` even though the generated binary was 82616 bytes. Talos now emits `__kernel_image_end - _start` in the Image header and `scripts/rpi5-image.sh` fails if the header size differs from the generated file size.
- 2026-05-19 local validation after the arm64 Image header correction passed: `cargo fmt --check`, `cargo -Zjson-target-spec test`, `cargo -Zjson-target-spec build --target targets/aarch64-talos-rpi5-bcm2712.json`, `./scripts/rpi5-image.sh`, `./scripts/qemu-smoke.sh`, and `mdbook build`. Header inspection showed `file=82616`, `header_size=82616`, and `text_offset=0x00200000`.
- 2026-05-19 lab API probe found no exposed TFTP log endpoint: `/boot/logs`, `/tftp/logs`, and `/logs/tftp` all returned 404. The existing exposed read-only evidence is `/status`, `/boot/files`, `/serial/peek`, and `/serial/tail`.
- 2026-05-19 added `scripts/rpi5-archive-review.sh` as a local pre-hardware gate. It checks required archive files, unsafe paths, `config.txt` first-light settings, `kernel_2712.img`/`kernel8.img` equality, arm64 Image magic, text offset, and header image size.
- 2026-05-19 corrected-image archive pre-hardware review passed for `target/talos-rpi5-boot.tar.gz`: sha256 `7c3994c313c5491414927d998c535522c8b1f920a2e608debafb4676fb7aadff`, required files present, `kernel_2712.img` and `kernel8.img` identical, `kernel_size=82616`, `header_image_size=82616`, and `text_offset=2097152`.
- 2026-05-19 corrected-image hardware attempt published archive digest `b437659350d6c110f056a664d17fb48a3dfb24117440e04013454f44b1918ab9`; `PUT /boot/archive` returned `ok=true`, `file_count=8`, and `extracted_bytes=252535`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `28313` to `29021` and emitted Pi firmware logs through `DDR 4267 1 0 64 152 BL:1`, then `RP1 FW: load 0` and `RP1_BOOT chip ID: 0x20001927`. No `T1` marker or Talos banner appeared.
- 2026-05-19 post-hardware review classified the corrected-image run as still failing before Talos entry. The next bounded hypothesis is that the inherited Linux `dtoverlay=uart0-pi5` is unnecessary for bare-metal first-light and may add avoidable firmware/device-tree work before entry. The staging script now strips that line and the archive review gate rejects it.
- 2026-05-19 minimal-config hardware attempt published archive digest `ad1c2f124015724f5026e9eb791405d296a546c59ba20f2712f18b827061f944`; `PUT /boot/archive` returned `ok=true`, `file_count=8`, and `extracted_bytes=252515`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `29021` to `29729` and emitted the same firmware/RP1 boundary ending in `RP1_BOOT chip ID: 0x20001927`. No `T1` marker or Talos banner appeared.
- 2026-05-19 post-hardware image-contract review compared Talos with the official Raspberry Pi `kernel_2712.img`. The official decompressed Pi 5 kernel image advertises arm64 Image `text_offset=0`, while Talos advertised `0x00200000`. The Pi 5 linker now uses `KERNEL_IMAGE_TEXT_OFFSET=0`, and the archive review gate expects `text_offset=0` for Pi 5.
- 2026-05-19 offset-zero hardware attempt published archive digest `a2b2fe61e41f0178f68437527e03a837d1cd21c9e10635b65c5ffa9aeac94116`; `PUT /boot/archive` returned `ok=true`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `29729` to `30437` and still stopped at the same firmware/RP1 boundary without `T1` or Talos output.
- 2026-05-19 follow-up image-contract review found another header mismatch: the official decompressed Pi 5 kernel image advertises arm64 Image flags `0xc`, while Talos advertised `0`. Talos now emits flags `0xc` for the Pi 5 image and the archive review gate checks that value.
- 2026-05-19 flags-matched hardware attempt published archive digest `22057bcd3614ebc871e3974a9f99bde524eb3241250105ca3343d1c07b21d462`; `PUT /boot/archive` returned `ok=true`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `30437` to `31145` and again stopped at the same firmware/RP1 boundary without `T1` or Talos output.
- 2026-05-19 post-hardware review: matching the official Pi 5 `text_offset` and flags did not change the failure boundary. The next useful direction is to determine whether the bootloader is actually reaching the TFTP/config/kernel load phase after RP1 firmware, or to try the Pi 5 `boot_ramdisk=1` path with a FAT32 `boot.img` so the network boot path receives a fuller boot filesystem shape.
- 2026-05-19 added `scripts/rpi5-boot-img.sh` and `scripts/rpi5-boot-ramdisk-tree.sh` to stage a documented Pi 5 `boot_ramdisk=1` network-boot experiment. The archive review gate now verifies `boot.img` is a readable FAT image containing the required config/kernel files when `boot_ramdisk=1` is enabled.
- 2026-05-19 boot-ramdisk hardware attempt published archive digest `4d6fc4527e6af0691167e974f4ca3e33c2afa1b3cf0ae8922e39c6e772e1bd94`; `PUT /boot/archive` returned `ok=true`, `file_count=9`, and `extracted_bytes=67361394`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `31145` to `31853` and again stopped at the same firmware/RP1 boundary without `T1`, Talos output, or visible `sha256=1` file-load hashes.
- 2026-05-19 post-hardware review: the unchanged boundary across direct-root, minimal-config, Pi5-header-matched, and `boot_ramdisk=1` archives suggests the next hypothesis is before or at the bootloader file-load/config phase rather than in Talos' Rust/BSS/stack path. Further Talos image-header tweaks have diminishing value without a way to see file-load progress or a different firmware entry path such as a custom armstub.
- 2026-05-19 added a custom armstub diagnostic path. `scripts/rpi5-armstub-diagnostic.sh` builds `armstub8-2712.bin` from a minimal AArch64 assembly stub that writes `S1\r\n` to firmware-preserved RP1 UART0 and waits. `scripts/rpi5-armstub-diagnostic-tree.sh` stages a one-off archive with `armstub=armstub8-2712.bin`, while the normal boot-tree path remains unchanged. The archive review gate now validates this optional armstub file when selected.
- 2026-05-19 armstub diagnostic hardware attempt published archive digest `c7fa87aa0d7b9989284e8bcfdd7ad09364c38619ea6eabd1531d4112a15ce8ea`; `PUT /boot/archive` returned `ok=true`, `file_count=9`, and `extracted_bytes=252645`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `31853` to `32561` and again stopped at the same firmware/RP1 boundary without `S1`, `T1`, or Talos output. This means either the network-boot firmware path did not reach the configured custom armstub, or the Pi 5 firmware ignored `armstub=armstub8-2712.bin` in this mode. The next bounded hypothesis is that the narrow Talos boot tree is missing some otherwise harmless firmware-stage files from the known-good Pi OS Lite tree, so a full-source boot tree with only the kernel/config deltas is worth testing before declaring the path blocked.
- 2026-05-19 added `scripts/rpi5-prefixed-boot-tree.sh` to mirror the required Talos boot files under `da591740/`, matching the Pi serial-prefix probe seen in the known-good TFTP request sequence. The archive review gate validates that the prefixed mirror is present and byte-identical when used.
- 2026-05-19 serial-prefix mirror hardware attempt published archive digest `aef985a35014cfffae791861c364a4776397569aaeca049c2b1cc891400f2628`; `PUT /boot/archive` returned `ok=true`, `file_count=16`, and `extracted_bytes=505030`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `32561` to `33269` and again stopped at the same firmware/RP1 boundary without `S1`, `T1`, or Talos output. This rules out the simplest root-vs-`da591740/` prefix fallback hypothesis for the current Talos archive shape.
- 2026-05-19 firmware/network-boot hypothesis review found one remaining low-cost archive-shape gap: the armstub diagnostic used only root files, while the serial-prefix mirror did not request `armstub8-2712.bin`. Added `scripts/rpi5-prefixed-armstub-diagnostic-tree.sh` to stage both `armstub=armstub8-2712.bin` and a byte-identical `da591740/` mirror, with archive-review checks for the mirrored armstub.
- 2026-05-19 combined serial-prefix plus armstub hardware attempt published archive digest `e16838e468ec5d07ff1a53a8fbfc5801446b6599e94edb25e6a752492a8279e7`; local gates passed at `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, `./scripts/rpi5-image.sh`, and archive review. `PUT /boot/archive` returned `ok=true`, `file_count=18`, and `extracted_bytes=505290`; `POST /power/cycle` returned `ok=true`; serial advanced from cursor `33269` to `33977` and again stopped at the same firmware/RP1 boundary without `S1`, `T1`, or Talos output.
- 2026-05-19 post-hardware review: the combined diagnostic rules out the remaining cheap root-vs-`da591740/` armstub/config archive-shape gap. The failure boundary is now before Talos entry and before the configured custom armstub can prove execution, or the Pi 5 network-boot firmware is not honoring that armstub/config path. Further kernel-image or archive-shape iterations are unlikely to create useful signal without lab-side TFTP request/file-load visibility, EEPROM boot diagnostics, or a recreated known-good Pi OS Lite boot source for direct comparison.
- 2026-05-19 lab API TFTP visibility run used the upgraded `/tftp/logs` endpoint around a controlled power cycle without republishing. Serial advanced from cursor `33977` to `34685` and still stopped at the firmware/RP1 boundary. Delayed TFTP logs proved the Pi requested and was served `da591740/config.txt`, `da591740/kernel_2712.img`, `da591740/bcm2712-rpi-5-b.dtb`, overlay files, `da591740/cmdline.txt`, and `da591740/armstub8-2712.bin`. This moves the boundary from "unknown file-load" to "files load, but neither the custom armstub marker nor kernel marker appears on the lab UART."
- 2026-05-19 review of the custom armstub found a concrete diagnostic flaw: the assembly marker disabled and re-enabled PL011 but did not program `IBRD`, `FBRD`, or `IMSC` like the Rust PL011 early init. Added those writes to both the armstub diagnostic and the Pi 5 assembly entry marker. Local gates passed: `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, `./scripts/rpi5-image.sh`, archive review, and QEMU smoke. Hardware attempt with archive digest `95ffda315ab53a17c2d66a4ef276b9a307b5f9a687c1a1cf07bbcb6c5f67197b` showed TFTP served the updated 128-byte `armstub8-2712.bin` and `kernel_2712.img`, but serial still stopped at the same boundary without `S1`, `T1`, or Talos output.
- 2026-05-19 added a preserved-UART marker before any PL011 reconfiguration: armstub emits `P0` first, then `S1`; kernel entry emits `P1` first, then `T1`. Local gates again passed. Hardware attempt with archive digest `62279261e7b5588cbcb340e96745ba991c28b213dd4d063b9de93f89cdc04876` showed TFTP served the updated 168-byte `armstub8-2712.bin` and `kernel_2712.img`, but serial still stopped at the same firmware/RP1 boundary without `P0`, `S1`, `P1`, `T1`, or Talos output.
- 2026-05-19 reference-implementation review found Circle's Pi 5 bare-metal config uses `kernel=kernel_2712.img` with `kernel_address=0x80000`. Talos now stages `kernel_address=0x80000` and links the Pi 5 image at `0x80000` while keeping arm64 Image `text_offset=0` and flags `0xc`. Local validation passed: `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, `./scripts/rpi5-image.sh`, symbol inspection showing `_start` and `__kernel_start` at `0x80000`, prefixed archive review, QEMU smoke, and `mdbook build`.
- 2026-05-19 Circle-style address plus custom armstub hardware attempt published archive digest `12f14dd0b2314fb926eb64d332faff5ff9ef7ea75a194d48a2d17e911d5ba67d`. `PUT /boot/archive` and `POST /power/cycle` returned `ok=true`; TFTP logs showed `da591740/config.txt`, `kernel_2712.img`, DTB, overlays, cmdline, and `armstub8-2712.bin` served, but serial still showed only the firmware/RP1 boundary with no `P0`, `S1`, `P1`, `T1`, or Talos output.
- 2026-05-19 Circle-style address without custom armstub hardware attempt published archive digest `d1f48d4a5e6c97a4554fda691140e6348c7ba26dd866e0b528eebe7057e2dad2`. `PUT /boot/archive` and `POST /power/cycle` returned `ok=true`; TFTP logs showed `da591740/config.txt`, `kernel_2712.img`, DTB, overlays, and cmdline served, plus a non-fatal `armstub8-2712.bin` not-found probe. Serial did not advance with Talos output. This rules out the custom armstub setting itself and the zero-vs-`0x80000` Pi 5 bare-metal load base as the immediate cause.
- 2026-05-19 Matthew corrected the workflow classification: the no-marker Pi 5 state is not blocked while bounded public-reference research, code review, loader diagnostics, or controlled hardware iterations remain.
- 2026-05-19 reference review compared Circle's Pi 5 bootloader with Talos. Circle stages a raw `kernel_2712.img` linked at `0x80000` rather than an arm64 Image-header payload. Added a separate raw loader diagnostic path: `scripts/rpi5-loader-diagnostic.sh` builds a 216-byte position-linked AArch64 `kernel_2712.img` that writes `L0` through firmware-preserved RP1 UART0, reinitializes RP1 UART0 and writes `L1`, also attempts a BCM2712 UART10 `U1` marker, then emits heartbeat dots. `scripts/rpi5-loader-diagnostic-tree.sh` and `scripts/rpi5-prefixed-loader-diagnostic-tree.sh` stage that raw image without changing the normal Talos boot path.
- 2026-05-19 local validation for the raw loader diagnostic passed: shell syntax checks, raw diagnostic build/disassembly, prefixed archive review with `loader_diagnostic=true`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, `./scripts/rpi5-image.sh`, `./scripts/qemu-smoke.sh`, and `mdbook build`.
- 2026-05-19 raw loader diagnostic hardware attempt published archive digest `b8c52c0736436fed53df9595b7b4e48c8aedb8e88deb055609bdd62c6b485e47`. `PUT /boot/archive` returned `ok=true`, `file_count=16`, and `extracted_bytes=175540`; `POST /power/cycle` returned `ok=true`. Serial advanced from cursor `36749` to `37457` through the same firmware/RP1 boundary and did not show `L0`, `L1`, `U1`, heartbeat dots, or Talos output. Full TFTP-log tail showed the Pi requested and was served the 216-byte `da591740/kernel_2712.img`, config, DTB, overlays, and cmdline at 17:35:23-17:35:24 UTC. This rules out the arm64 Image header and Rust/BSS/stack setup as the only explanation for the no-marker state; the next useful path is an execution proof that does not depend on the lab-visible RP1 UART path, or a deeper reference-backed handoff/exception-state diagnostic.
- 2026-05-19 follow-up review found the first raw loader diagnostic could still hang or abort before reaching its alternate UART attempt because it touched RP1 UART0 first. The raw loader diagnostic now installs a same-EL exception vector, skips aborting MMIO instructions by advancing `ELR_EL1` or `ELR_EL2`, and tries BCM2712 UART10 before the RP1 UART path.
- 2026-05-19 exception-safe raw loader diagnostic local validation passed: shell syntax, diagnostic build/disassembly, prefixed archive review with `loader_diagnostic=true`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, `./scripts/rpi5-image.sh`, `./scripts/qemu-smoke.sh`, and `mdbook build`.
- 2026-05-19 exception-safe raw loader hardware attempt published archive digest `750fc6a4099bd316b4dd4e7edf9c7ad00267f45b0bdf637f649e39058e5ab777`. `PUT /boot/archive` returned `ok=true`, `file_count=16`, and `extracted_bytes=190868`; `POST /power/cycle` returned `ok=true`. Serial advanced from cursor `37457` to `38165` through the same firmware/RP1 boundary and did not show `U1`, `L0`, `L1`, heartbeat dots, or Talos output. TFTP logs showed the Pi requested and was served the 4048-byte `da591740/kernel_2712.img` plus config, DTB, overlays, and cmdline at 17:41:59-17:42:00 UTC. This keeps the workflow unblocked but shifts the next useful diagnostic away from simple MMIO-abort avoidance and toward proving firmware CPU handoff or using a different observable side effect.
- 2026-05-19 reference review found Circle's Raspberry Pi watchdog reset path for Pi 5 uses power-manager registers at `ARM_IO_BASE + 0x1200000`, with `ARM_IO_BASE=0x107c000000`. Added a watchdog-reset side effect to the raw loader diagnostic after its UART attempts: write `0x5a000001` to `0x107d200024` and `0x5a000020` to `0x107d20001c`, based on Circle's `reboot()` / `CBcmWatchdog` sequence. This is meant to prove execution even if UART is not observable.
- 2026-05-19 watchdog raw loader local validation passed: diagnostic build/disassembly, prefixed archive review with `loader_diagnostic=true` and local `kernel_size=4076`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, `./scripts/rpi5-image.sh`, `./scripts/qemu-smoke.sh`, and `mdbook build`.
- 2026-05-19 watchdog raw loader hardware attempt published archive digest `f62b7a68dd2eb7ed9b5a25c9b3f126e277876b1cb316b03b40204abf606197c1`. `PUT /boot/archive` returned `ok=true`, `file_count=16`, and `extracted_bytes=190980`; `POST /power/cycle` returned `ok=true`. Serial advanced from cursor `38165` to `38873` through the same firmware/RP1 boundary and showed no `U1`, `L0`, `L1`, `W0`, heartbeat, Talos output, or second firmware boot from a watchdog reset during a 55-second observe window. TFTP logs showed the Pi requested and was served the watchdog archive's prefixed config/kernel/DTB/overlays/cmdline at 17:57:12-17:57:13 UTC. The archive was rolled back immediately after the observation to avoid leaving a potential reset-loop diagnostic staged.
- 2026-05-19 added a PSCI `SYSTEM_RESET` SMC call before the watchdog MMIO reset in the raw loader diagnostic. This tests a firmware/monitor-mediated side effect that does not depend on RP1 UART visibility or BCM2712 watchdog MMIO accessibility.
- 2026-05-19 PSCI raw loader local validation passed: raw diagnostic build/disassembly showed `smc #0` in `trigger_psci_reset`, prefixed archive review passed with `loader_diagnostic=true`, `kernel_size=4088`, and archive digest `8c035e645bce113b6d1bb00a0f97e69e3b2d30e8cebcb763aff78c4e321099df`; `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, `./scripts/rpi5-image.sh`, `./scripts/qemu-smoke.sh`, and `mdbook build` all passed.
- 2026-05-19 PSCI raw loader hardware attempt published archive digest `8c035e645bce113b6d1bb00a0f97e69e3b2d30e8cebcb763aff78c4e321099df`. `PUT /boot/archive` and `POST /power/cycle` returned `ok=true`; serial advanced from cursor `38873` to `39581` through the same firmware/RP1 boundary and showed no `U1`, `L0`, `L1`, `W0`, heartbeat, Talos output, or second firmware boot from PSCI/reset side effects during the 55-second observe. The TFTP cursor query returned no new delta despite the serial reboot; the tail still shows the earlier prefixed loader runs, so this run's file-load evidence is serial/publish based rather than a fresh TFTP delta. The archive was rolled back immediately after observation.
- 2026-05-19 added a UEFI intermediate-loader diagnostic. `scripts/rpi5-efi-diagnostic.sh` builds a minimal AArch64 PE/COFF EFI application that writes `Talos EFI first-light PASS` through `EFI_SIMPLE_TEXT_OUTPUT_PROTOCOL.OutputString`; `scripts/rpi5-efi-diagnostic-fat.sh` stages it as `EFI/BOOT/BOOTAA64.EFI` on a FAT image with `startup.nsh`; `scripts/rpi5-efi-qemu-smoke.sh` boots it under QEMU virt plus AAVMF/QEMU_EFI.
- 2026-05-19 UEFI diagnostic local/substitute validation passed: `file` identified the app as `PE32+ executable (EFI application) Aarch64`, FAT staging contained `EFI/BOOT/BOOTAA64.EFI` and `startup.nsh`, QEMU/AAVMF printed `Talos EFI first-light PASS`, and the normal Talos gates still passed: `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, `./scripts/rpi5-image.sh`, and `./scripts/qemu-smoke.sh`. This remains a substitute validation artifact only; Talos should not depend on an external bootloader to run it on hardware.
- 2026-05-19 Matthew clarified that Talos should develop its kernel from scratch including its own bootloader. The U-Boot staging direction was removed from the active path; external projects may be inspected as references for hardware contracts and boot sequence, but Talos-owned loader code should provide the hardware proof.
- 2026-05-19 added a Circle-style minimal-config raw loader diagnostic. `scripts/rpi5-circle-loader-diagnostic-tree.sh` stages the existing Talos-owned raw loader under a minimal Pi 5 config modeled on Circle's `config64.txt`: `arm_64bit=1`, `kernel_address=0x80000`, `initial_turbo=0`, `[pi5]`, and `kernel=kernel_2712.img`, with the required DTB/overlay files and a `da591740/` mirror. This intentionally omits the Talos first-light `enable_rp1_uart=1` and `pciex4_reset=0` settings to test whether those diagnostic settings affect firmware handoff.
- 2026-05-19 Circle-style minimal-config raw loader local validation passed: shell syntax, raw loader build, archive review with `loader_diagnostic=true`, `circle_config_loader_diagnostic=true`, `kernel_size=4088`, and archive digest `2373703743cd87384ad7724d2ce4a358e2fc9ffc6986e8ef447ddb5d0b82fc69`; `cargo fmt --check`, `cargo -Zjson-target-spec test`, raw loader build, QEMU smoke, and `mdbook build` all passed.
- 2026-05-19 Circle-style minimal-config raw loader hardware attempt published archive digest `2373703743cd87384ad7724d2ce4a358e2fc9ffc6986e8ef447ddb5d0b82fc69`. `PUT /boot/archive` and `POST /power/cycle` returned `ok=true`; serial advanced from cursor `39581` to `40289` through the same firmware/RP1 boundary and showed no `U1`, `L0`, `L1`, `W0`, heartbeat, Talos output, or second firmware boot from PSCI/watchdog side effects during a 75-second observe. The TFTP cursor/tail query did not expose a fresh delta for this run. The archive was rolled back after observation.

## Review

- Pre-hardware review findings: passed for one controlled attempt. The archive uses only explicit relative paths, includes the lab-controller required Pi 5 files, selects `kernel_2712.img`, enables RP1 UART0 preservation, and has rollback available.
- Hardware test evidence: archive publish and one controlled power-cycle succeeded. Serial confirms the Pi rebooted far enough to emit RP1 firmware messages on the lab cable, but Talos entry output was not observed. Rollback and recovery power-cycle also succeeded, returning the board to the Pi OS Lite boot tree.
- Post-hardware review findings: failed acceptance. The first hardware run proved reboot, RP1 UART visibility, rollback, and recovery, but did not prove Talos entry. The Pi 5 physical link base has been corrected and an assembly entry marker has been added for the next valid hardware run.

## Result

The first Talos boot archive was published to the lab TFTP root and controlled reboots were executed successfully through the fixed-port lab API. The observed serial output proves the lab cable and RP1 firmware path are active during boot, but it does not prove Talos reached entry. Local review found and corrected the Pi 5 link-address mismatch and the RP1 UART0 mapping. Subsequent hardware attempts with a direct assembly UART marker still stop after the firmware DDR log boundary, before any Talos marker appears. The next investigation should determine whether Pi firmware is reading the Talos TFTP boot tree after DDR init, whether a `boot.img`/full boot-tree shape is required for this network path, or whether the raw `kernel_2712.img` image format/header is rejected before entry.

## Follow-Up

- Add lab API visibility for TFTP requests or bootloader file-load logs; current serial evidence stops before confirming that `config.txt`, `kernel_2712.img`, or `kernel8.img` are requested after DDR init.
- Re-check whether this network boot path needs a `boot.img` ramdisk or fuller Pi OS boot tree shape even though Pi 5 embeds the old `start.elf` role in EEPROM firmware.
- Run one controlled custom-armstub hardware diagnostic. Seeing `S1` means firmware honored the armstub path before the kernel handoff; not seeing it keeps the failure boundary before that diagnostic entry.
- Test a full-source boot tree variant that preserves the known-good Pi OS Lite boot file shape while replacing the kernel images and first-light config lines.
- Run one controlled serial-prefix mirror hardware diagnostic.
- Review remaining firmware/config-entry hypotheses before another hardware run. A combined prefix-plus-armstub archive is a possible narrow experiment, but it should first be weighed against Raspberry Pi EEPROM/network-boot documentation and any lab-side way to recover TFTP request visibility.
- Combined prefix-plus-armstub diagnostic completed and stopped at the same boundary.
- TFTP request/file-load visibility now proves the Pi is being served the prefixed config, kernel, DTB, overlays, cmdline, and armstub files.
- The remaining productive path is deeper comparison with public Pi 5 firmware CPU-handoff behavior, a loader path that is known to run under Linux/UEFI before switching to bare metal, or another observable side effect that does not rely on BCM2712/RP1 MMIO being reachable after firmware handoff.
- Inspect the remaining staged `config.txt`, `cmdline.txt`, and `kernel_2712.img` contract against Raspberry Pi 5 firmware expectations, especially whether the raw ELF-stripped binary is acceptable as `kernel_2712.img` in this network boot path.
- Restore or recreate a known-good Pi OS Lite TFTP boot tree source before extended repeated publish cycles; the lab API keeps only one rollback archive, so repeated Talos publishes can displace the known-good rollback.
- If the EFI diagnostic remains useful, run it through a Talos-owned loader path rather than an external UEFI bridge.
- Continue developing a minimal Talos-owned bootloader/loader diagnostic that proves execution before Talos proper, using public implementations only to understand hardware contracts.
- Circle-style minimal config did not change the direct-firmware failure boundary; next Talos-owned work should inspect firmware handoff assumptions that are independent of optional first-light config settings.
- 2026-05-19 Raspberry Pi Linux reference review pulled a sparse `raspberrypi/linux` checkout at `d0de390` and found a concrete RP1 UART0 address hypothesis: `rp1.dtsi` declares UART0 at RP1 bus `0xc0_40030000`, and `bcm2712.dtsi` maps pcie2 32-bit non-prefetchable space to CPU physical `0x1f00000000`. The raw loader now tries `0x1f00030000` markers `N0`/`N1` before falling back to the earlier `0x1c00030000` `L0`/`L1` probes.
- 2026-05-19 Linux-derived RP1 UART0 hardware attempt published archive digest `b5cb364106dae20de1a61a25fed66ef1df9f36023362ec1c443f34b44205dc90`. Local gates passed before publish: `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, raw loader marker inspection, archive review, QEMU smoke, mdBook, and diff check. `PUT /boot/archive` and `POST /power/cycle` returned `ok=true`; delayed TFTP logs proved the Pi was served the 4096-byte `da591740/kernel_2712.img`, config, DTB, overlays, and cmdline at 19:06:48-19:06:49 UTC. Serial advanced from cursor `40289` to `40997` through the same Raspberry Pi firmware/RP1 boundary and showed no `N0`, `N1`, `L0`, `L1`, `U1`, `W0`, heartbeat dots, Talos output, or reset side effect during the 75-second observe. `POST /boot/rollback` restored the previous archive after observation.
- 2026-05-19 debug-config plus Linux-derived RP1 UART0 hardware attempt published archive digest `4831f8acdfab1b9303c78f062190e2f149b85363f11b902b28097140bc845ff4`. Local gates passed before publish: `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, raw marker inspection, archive review, QEMU smoke, mdBook, and diff check. This run used the normal Talos first-light debug config (`os_check=0`, `enable_rp1_uart=1`, `pciex4_reset=0`, `uart_2ndstage=1`, `sha256=1`) with the updated raw loader. `PUT /boot/archive` and `POST /power/cycle` returned `ok=true`; serial advanced from cursor `40997` to `41705` through the same Raspberry Pi firmware/RP1 boundary and showed no `N0`, `N1`, `L0`, `L1`, `U1`, `W0`, heartbeat dots, Talos output, or reset side effect during the 85-second observe. The TFTP cursor did not expose a fresh delta for this run, so evidence is publish/power-cycle/serial only. `POST /boot/rollback` restored the previous archive.
- 2026-05-19 EL3 exception-vector raw-loader attempt added `VBAR_EL3` setup and `ELR_EL3` skip handling so an EL3 firmware entry would not bypass the diagnostic's same-EL abort recovery. Local gates passed before publish: raw-loader build, disassembly inspection showing `VBAR_EL3`/`ELR_EL3`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, raw marker inspection, archive review, QEMU smoke, mdBook, and diff check. Hardware archive digest `a1485244630ceef8f74f06d68a23717eab1415d4ba0475010f7b920b520f88d0` published and power-cycled successfully; serial advanced from cursor `41705` to `42355` through Raspberry Pi firmware logs but showed no `N0`, `N1`, `L0`, `L1`, `U1`, `W0`, heartbeat dots, Talos output, or reset side effect. Recent TFTP logs showed the normal config, kernel, DTB, overlay, and cmdline requests at boot time. Rollback restored the previous archive.
- 2026-05-19 Matthew clarified to simplify first-light work to an assembly-only UART proof before adding more loader complexity. Added `src/arch/aarch64/rpi5_uart_proof.S` plus staging scripts. The proof image is 144 bytes, preserves `x0` in `x19`, parks secondary cores with `MPIDR_EL1`, initializes one PL011 path at Linux/Circle's RP1 UART0 physical address `0x1f00030000`, and repeatedly writes `TA\r\n`; it avoids Rust, stack setup, BSS clearing, exception vectors, PSCI, watchdog, and multi-UART fallback logic. Local gates passed: shell syntax, proof image build, disassembly inspection, marker-byte inspection, archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and diff check. Hardware archive digest `3e405afa92020ca74c02d9e64c2b8f79711be31122b88bd21c1f2d9819f4c17b` published and power-cycled successfully; serial advanced from cursor `42355` to `43005` through Raspberry Pi firmware logs but showed no repeated `TA` marker. The TFTP cursor did not expose a fresh delta for this run, so evidence is publish/power-cycle/serial only. A repeated rollback check toggled the one-archive rollback back to the 144-byte proof tree; the previous EL3 diagnostic archive `a1485244630ceef8f74f06d68a23717eab1415d4ba0475010f7b920b520f88d0` was republished without power-cycling to leave the lab boot tree off the tiny proof archive.
- 2026-05-19 serial-prefixed assembly UART proof added `scripts/rpi5-prefixed-uart-proof-tree.sh` to mirror the same 144-byte proof image and boot files under `da591740/` without changing the assembly. Local gates passed: shell syntax, proof image build, disassembly inspection, marker-byte inspection, prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and diff check. Hardware archive digest `ea6bf1f7c94d7a175ae018872980c0d0d6d2bf7300a99b2dceffa274f4e923e2` published and power-cycled successfully; serial advanced from cursor `43005` to `43655` through Raspberry Pi firmware logs but showed no repeated `TA` marker. The TFTP cursor again did not expose a fresh delta for this run, so evidence is publish/power-cycle/serial only. A repeated rollback check toggled the one-archive rollback back to the 144-byte proof tree; the previous EL3 diagnostic archive `a1485244630ceef8f74f06d68a23717eab1415d4ba0475010f7b920b520f88d0` was republished without power-cycling, and status confirmed the 4120-byte previous diagnostic tree.
- 2026-05-19 firmware-preserved-map assembly UART proof changed only the 144-byte proof's UART base literal from Linux/Circle's RP1 UART0 physical `0x1f00030000` to the firmware-preserved RP1 UART mapping `0x1c00030000`, keeping the serial-prefixed archive shape and all other proof logic unchanged. Local gates passed: shell syntax, proof image build, disassembly inspection showing the `0x1c` high word literal, marker-byte inspection, prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and diff check. Hardware archive digest `cf3ec38488a46752a52a27893a73bafecb1daf83590f0e20f17e0c606e131f80` published and power-cycled successfully; serial advanced from cursor `43655` to `44305` through Raspberry Pi firmware logs but showed no repeated `TA` marker. The TFTP cursor again did not expose a fresh delta for this run, so evidence is publish/power-cycle/serial only. The previous EL3 diagnostic archive `a1485244630ceef8f74f06d68a23717eab1415d4ba0475010f7b920b520f88d0` was republished without power-cycling afterward, and status confirmed the 4120-byte previous diagnostic tree.
- 2026-05-19 serial10 assembly UART proof changed only the same 144-byte proof's UART base literal to BCM2712 UART10 / Pi 5 debug UART `0x107d001000`, based on the firmware `serial10` reference note and Talos' Pi 5 target map. Local gates passed: shell syntax, proof image build, disassembly inspection showing the `0x107d001000` literal, marker-byte inspection, prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and diff check. Hardware archive digest `4099e52e8bc660727dd0081ddd332453e4e0b438b118065ec5c05a575f8c9f82` published and power-cycled successfully; serial advanced from cursor `44305` to `44955` through Raspberry Pi firmware logs but showed no repeated `TA` marker. TFTP follow-up did not show a current 144-byte serial10 image fetch and instead showed repeated earlier 4120-byte diagnostic fetches, so this result is publish/power-cycle/serial evidence rather than fresh file-load proof. The previous EL3 diagnostic archive `a1485244630ceef8f74f06d68a23717eab1415d4ba0475010f7b920b520f88d0` was republished without power-cycling afterward, and status confirmed the 4120-byte previous diagnostic tree.
- 2026-05-19 RP1 GPIO-mux assembly UART proof added only the Linux-derived 40-pin-header setup before the PL011 write loop: GPIO14 pad input-enable/no-pull, GPIO15 pad input-enable/pull-up, GPIO14/GPIO15 control function select 4 for `uart0`, then RP1 UART0 at `0x1f00030000`. Reference basis: Linux RP1 DTS maps GPIO14/15 to `rp1_uart0_14_15`, and the RP1 pinctrl driver places bank0 pad/control registers at bus `0xc0_400f0000` / `0xc0_400d0000`, which map to CPU physical `0x1f000f0000` / `0x1f000d0000` through the pcie2 window. The proof remains assembly-only and avoids Rust, stack, BSS, exception vectors, reset side effects, and multi-UART fallback; size is now 224 bytes. Local gates passed: shell syntax, proof image build, disassembly inspection showing the pad/control/UART literals, marker-byte inspection, prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and diff check. Hardware archive digest `323e972a2da9bb56552d0c0a6d3abbd742e2f543d2bdbfe5a26cfbd3f29ef479` published and power-cycled successfully; serial advanced from cursor `44955` to `45667` through Raspberry Pi firmware logs and reached `NETWORK: ... wait for link TFTP: 0.0.0.0`, but showed no repeated `TA` marker. TFTP follow-up did not capture a current 224-byte proof-image fetch before rollback, so this result is publish/power-cycle/serial evidence rather than fresh file-load proof. The previous EL3 diagnostic archive `a1485244630ceef8f74f06d68a23717eab1415d4ba0475010f7b920b520f88d0` was republished without power-cycling afterward, and status confirmed the 4120-byte previous diagnostic tree.
- 2026-05-19 validation-protocol review found the cause of the stale TFTP follow-up deltas: `/tftp/logs?cursor=0&limit=1` silently returns a 64 KiB truncated window by default, so its `cursor_end` was `65536` while the real lab TFTP EOF cursor was already beyond `215000`. Added `scripts/rpi5-tftp-cursor.sh` to request a full 1 MiB window and return the current EOF cursor before hardware tests. This does not change the no-`TA` serial outcome, but it means the next hardware iteration can correctly prove whether the current archive's config/kernel files were fetched before rollback.
- 2026-05-19 corrected-cursor rerun of the same 224-byte RP1 GPIO-mux assembly UART proof published archive digest `8b20390af6861e91c775b96c23dc81651a51745e087dcfc9f0c74304c1129843` and power-cycled successfully. Serial advanced `45667->46315` through early Raspberry Pi firmware/DDR logs with no `TA` marker, but the follow-up TFTP query showed no new events before rollback because `/serial/observe` returned after the first firmware burst, before the network/TFTP phase. Added `scripts/rpi5-wait-tftp-delta.sh` so the next controlled hardware run waits for fresh TFTP events, or a bounded timeout, before rollback.
- 2026-05-19 wait-for-TFTP rerun of the same 224-byte RP1 GPIO-mux assembly UART proof published archive digest `cf4b1cbb232f50b5134a4aec44084e7efc261306a7174f994338fd817863a68a` and power-cycled successfully. Serial advanced `46315->46965` through Raspberry Pi firmware/DDR logs and a trailing NUL, but showed no repeated `TA` marker. The corrected TFTP evidence captured cursor `350002->351353` with 13 fresh events, including served `da591740/config.txt`, `da591740/kernel_2712.img` at 224 bytes twice, `da591740/bcm2712-rpi-5-b.dtb`, overlays, and `da591740/cmdline.txt`, plus expected not-found probes for `pieeprom.sig` and `armstub8-2712.bin`. This confirms the current tiny assembly image is fetched and selected; the failure remains between firmware file load and lab-visible UART marker.
- 2026-05-19 non-hardware handoff review compared the tiny proof with the Linux arm64 boot image contract. The proof is still assembly-only and still branches directly to the marker code, but it now starts with a minimal arm64 Image header: branch instruction, `text_offset=0`, exact file-size `image_size`, flags `0xc`, and magic `ARMd`. This keeps the next hardware hypothesis inside the simplification policy while testing whether Pi 5 firmware is stricter about the kernel image contract than Circle's raw-loader examples imply. Local validation passed for proof build, header inspection, archive review, mdBook, and diff check; no hardware run was performed for this change.
- 2026-05-19 headered assembly UART proof hardware attempt published archive digest `6fb98f25ed3d43aaf501cd156e8fa523d00ff4f76ee515bcbb44d1a6666079b6` and power-cycled successfully. Local gates passed before publish: proof build, header inspection showing `kernel_size=288`, `header_image_size=288`, `text_offset=0`, `flags=12`, prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, and mdBook. Corrected TFTP evidence captured cursor `351353->352704` with 13 fresh events, including served `da591740/kernel_2712.img` at 288 bytes twice. Serial observed only `\\0\\n` from cursor `46965->46967` and no repeated `TA` marker. Rollback restored the previous boot tree hash `02a1311a6419ca764a2b19b2a34e4ad1b71e74972c6484e7b5620fc8018ec7d9`. This rules out the missing arm64 Image header as the immediate reason the fetched tiny proof does not produce lab-visible UART output.
- 2026-05-19 non-hardware handoff review found that the latest headered proof still writes RP1 pad/control registers before the first UART byte, so a bad RP1 MMIO assumption can abort the proof before the lab-visible marker. The proof was simplified again to rely only on firmware-preserved RP1 UART0: no GPIO mux writes, no PL011 reinitialization writes, UART base `0x1c00030000`, TX-full polling, and repeated `TA\r\n`. This keeps the Image header and assembly-only shape while making the next hardware test a single-premise check of the firmware-preserved UART contract with corrected TFTP evidence.
- 2026-05-19 firmware-preserved UART-only proof hardware attempt published archive digest `f839443aae5673677d2cf4f571517347d8efc5b666309b426567191a1c7c05e6` and power-cycled successfully. Local gates passed before publish: proof build, disassembly review showing no RP1 GPIO pad/control literals and the literal-pool `0x1c00030000` UART base, marker-byte check, prefixed archive review with `kernel_size=152`, `header_image_size=152`, `text_offset=0`, `flags=12`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and `git diff --check`. Corrected TFTP evidence captured cursor `352704->354055` with 13 fresh events, including served `da591740/kernel_2712.img` at 152 bytes twice. Serial advanced `46967->47389` and reached `Starting OS` plus `NOTICE: BL31: v2.6...` messages, but no repeated `TA` marker. Rollback restored the previous boot tree hash `02a1311a6419ca764a2b19b2a34e4ad1b71e74972c6484e7b5620fc8018ec7d9`. This makes TFTP/archive shape and pre-UART GPIO faults lower-probability causes; the next review should focus on the BL31/ATF handoff contract and what address/EL/state receives control after `Starting OS`.
- 2026-05-19 BL31/ATF handoff review identified one remaining pre-UART branch in the assembly proof: it read `MPIDR_EL1` and parked unless `AFF0 == 0`. The proof now removes MPIDR filtering and secondary-core parking so the entry CPU writes `TA\r\n` unconditionally through firmware-preserved RP1 UART0. This is intentionally less disciplined for SMP, but it is the smallest way to test whether the BL31-entered CPU was being parked before first light.
- 2026-05-19 no-MPIDR UART proof hardware attempt published archive digest `ddf1c574c3470c608f7a2618b69243d4b8012575bb81c40f9f12f2e7e6805160` and power-cycled successfully. Local gates passed before publish: proof build, disassembly review showing no `MPIDR` or `wfe`, literal-pool `0x1c00030000` UART base present, marker-byte check, prefixed archive review with `kernel_size=136`, `header_image_size=136`, `text_offset=0`, `flags=12`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and `git diff --check`. Corrected TFTP evidence captured cursor `354055->355406` with 13 fresh events, including served `da591740/kernel_2712.img` at 136 bytes twice. Serial observed only a NUL/newline from cursor `47389->47391` and no repeated `TA` marker. Rollback restored the previous boot tree hash `02a1311a6419ca764a2b19b2a34e4ad1b71e74972c6484e7b5620fc8018ec7d9`. This makes the MPIDR core filter unlikely as the immediate no-marker cause; continue with BL31/ATF handoff and entry-address/state review before the next hardware run.
- 2026-05-19 BL31/ATF handoff review compared Raspberry Pi Pi 5 boot docs, the Linux arm64 boot ABI, and TF-A's Pi 5 port. The firmware/BL31 path should pass control to a preloaded 64-bit BL33 payload at the firmware-provided kernel address with `x0` as the DTB pointer, so the remaining pre-output instruction in the tiny proof was the PL011 flag-register poll. The proof now removes TX-full polling and writes `TA\r\n` directly to the firmware-preserved RP1 UART0 data register at `0x1c00030000`. Local gates passed: proof build, disassembly review showing no flag polling, `MPIDR`, or `wfe`, marker-byte check, prefixed archive review with `kernel_size=128`, `header_image_size=128`, `text_offset=0`, `flags=12`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and `git diff --check`.
- 2026-05-19 direct-DR UART proof hardware attempt published archive digest `0fef9e02a43559e85060048eda0a101cb1507f14200c243f9a761c67913a7b1f` and power-cycled successfully. Corrected TFTP evidence captured cursor `355406->356564` with 11 fresh events, including served `da591740/kernel_2712.img` at 128 bytes. Serial observed only a NUL/newline from cursor `47391->47393` and no repeated `TA` marker. A publish retry displaced the one-archive rollback slot, so the previous 4120-byte EL3 diagnostic tree was republished without another power-cycle; status confirmed restored tree hash `02a1311a6419ca764a2b19b2a34e4ad1b71e74972c6484e7b5620fc8018ec7d9`. This makes PL011 flag polling unlikely as the immediate no-marker cause.
- 2026-05-19 non-hardware BL31/kernel-entry review added a 96-byte assembly-only `asm-entry-reset-proof` diagnostic to separate bad BL33 entry from unavailable UART. The image keeps the minimal arm64 Image header, preserves `x0`, performs no UART or other MMIO, and repeatedly invokes PSCI `SYSTEM_RESET` with `smc #0`. Local static/archive validation passed: shell syntax, proof build, disassembly inspection showing `smc #0` and no UART/MMIO writes, prefixed archive review with `kernel_size=96`, `header_image_size=96`, `text_offset=0`, and `flags=12`. Hardware acceptance for this diagnostic is a fresh 96-byte `da591740/kernel_2712.img` fetch plus a second firmware boot or repeated TFTP sequence without another lab power-cycle.
- 2026-05-19 entry-reset proof hardware attempt published archive digest `6429ebf9ca33b4443dd75719a5babccfc1ddfade78ec551977685be9ed20d4f4` and power-cycled successfully. A pre-run boot snapshot `pre-entry-reset-20260519231251` was captured and restored afterward. Corrected TFTP evidence captured cursor `363319->364670` with 13 fresh events, including served `da591740/kernel_2712.img` at 96 bytes twice. Serial advanced `48045->48265` through one firmware/RP1 boot fragment and no Talos output. No repeated TFTP sequence or clear second firmware boot appeared before restore; status confirmed the previous 4120-byte diagnostic tree hash `02a1311a6419ca764a2b19b2a34e4ad1b71e74972c6484e7b5620fc8018ec7d9` after restore. This does not prove BL33 is never entered, but it means a no-MMIO PSCI reset side effect also does not produce a visible execution signal.
- 2026-05-19 non-hardware entry-contract review added `scripts/rpi5-entry-reset-firmware-address-tree.sh`, a variant that stages the same 96-byte no-MMIO PSCI reset proof but removes forced `kernel_address=0x80000` so Pi 5 firmware chooses the Image placement. Local validation passed for shell syntax, absence of `kernel_address` in the staged config, and archive review with `kernel_size=96`, `header_image_size=96`, `text_offset=0`, and `flags=12`. This tests whether the current forced address conflicts with the firmware/BL31 Image entry calculation.
- 2026-05-19 firmware-address entry-reset hardware attempt published archive digest `75e2dac0e3fa75bd2866c655cade5abbb801e78aaed32fd13f2a163f7cc179dc` and power-cycled successfully. A pre-run snapshot `pre-fwaddr-reset-20260519232742` was captured and restored afterward. The first captured TFTP delta `421412->422763` served `da591740/kernel_2712.img` at 96 bytes twice from the no-`kernel_address` proof tree; a second firmware TFTP sequence appeared at 23:28:23/24 before restore. This satisfies the narrow no-UART BL33-entry discriminator and shifts the next bounded step back to a UART marker proof staged without forced `kernel_address=0x80000`.
- 2026-05-19 added `scripts/rpi5-uart-proof-firmware-address-tree.sh`, which stages the current assembly-only UART proof with the same firmware-selected Image placement that made the reset proof visibly execute. The diagnostic keeps the serial-prefixed mirror and removes `kernel_address=0x80000`; local validation for this archive is required before the next single hardware run.
- 2026-05-19 firmware-address UART proof hardware attempt published archive digest `2af5611a7fdb49821c7488700e60b452390a6a29cdb7a160d385f5a36410f9f9` and power-cycled successfully. A pre-run snapshot `pre-fwaddr-uart-20260519234409` was restored afterward. Published status showed `da591740/config.txt` at 174 bytes without forced `kernel_address` and `da591740/kernel_2712.img` at 128 bytes. TFTP cursor `492185->492649` served the 128-byte kernel plus DTB/overlays; serial advanced `50237->50945` through firmware/RP1 logs with no repeated `TA` marker. This keeps BL33 entry plausible from the reset proof but makes firmware-preserved RP1 UART0 at `0x1c00030000` insufficient as the next first-light assumption.
- 2026-05-19 updated the firmware-address UART proof to add only RP1 GPIO14/GPIO15 pad/control writes for UART0, then directly write `TA\r\n` to RP1 UART0 at `0x1f00030000`. This reruns the GPIO-mux hypothesis under the firmware-selected Image placement that made the reset proof execute, while still avoiding PL011 reinitialization, flag polling, MPIDR filtering, and fallback UART paths.
- 2026-05-19 firmware-address GPIO-mux UART proof hardware attempt published archive digest `061191ba3d3b9993cfdc29ccc388ce600950b2cd413dc8dbcf647e3646bd4033` and power-cycled successfully. A pre-run snapshot `pre-fwaddr-gpio-uart-20260519235644` was restored afterward. TFTP cursor `546689->548040` served the 208-byte `da591740/kernel_2712.img` twice plus config, DTB, overlays, and cmdline. Serial advanced `50945->52445` through firmware/RP1 logs, `Starting OS`, and BL31 notices, but no repeated `TA` marker appeared. This makes both firmware-preserved UART0 and minimal RP1 GPIO-mux direct-write UART0 insufficient under the now-working Image placement.
- 2026-05-20 added `asm-uart-then-reset-firmware-address`, a 136-byte assembly-only proof that uses firmware-selected Image placement, writes `TA\r\n` once to firmware-preserved RP1 UART0 at `0x1c00030000`, then invokes PSCI `SYSTEM_RESET`. Local validation passed: shell syntax, archive review, Image header inspection, disassembly showing the UART literal and `smc #0`, marker-byte inspection, no forced `kernel_address`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and `git diff --check`.
- 2026-05-20 UART-then-reset hardware attempt published archive digest `9c830903652c9662f99eccc282e1b18459f65bd61ed0c0aef995fcbd70d4e829` and power-cycled successfully. A pre-run snapshot `pre-uart-then-reset-20260520001342` was restored afterward. TFTP cursor `548040->552093` captured 39 fresh events with three repeated boot sequences, each serving the 136-byte `da591740/kernel_2712.img`. Serial advanced `52445->53093` through early Raspberry Pi firmware/DDR logs but showed no `TA` marker. This proves execution continued past the UART stores to the PSCI reset side effect, so the no-marker issue is now localized to UART visibility/routing/clock/reset/register semantics rather than firmware handoff, Image placement, or direct-store abort.
- 2026-05-20 Pi 5 first-light UART proof succeeded. The working 272-byte assembly-only Image uses firmware-selected placement, emits an initial marker to BCM2712 `uart10`, configures RP1 GPIO14/GPIO15 for UART0, flushes each RP1 pin-control write with a readback, and flushes each UART data-register write by reading the PL011 flag register. Local validation passed: archive review with `kernel_size=272`, Image header `text_offset=0`, `flags=12`, disassembly showing the `0x107d001000`, RP1 pad/control, and `0x1f00030000` literals plus readbacks, no forced `kernel_address`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, Pi 5 target build, QEMU smoke, mdBook, and `git diff --check`.
- 2026-05-20 first-light hardware evidence: the lab published and power-cycled the proof, TFTP served the selected 272-byte image, serial reached BL31 handoff, and the 40-pin header printed repeated `TA` markers. A later non-destructive serial peek still showed repeated `TA` after BL31 with current boot tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`. This satisfies the Phase 2.1 first-light criterion at serial hardware boot/output level and identifies RP1 PCIe posted-write flushing as the key hardware rule to preserve in the real Pi 5 UART driver.
- 2026-05-20 Rust-sized padded proof hardware evidence: an 83,304-byte assembly-only Image with the successful RP1 readback-flush UART sequence was served as `da591740/kernel_2712.img` twice and printed repeated `TP` after BL31. This rules out raw Image size or TFTP transfer length as the reason the normal 83,304-byte Rust image currently fails to print.
- 2026-05-20 normal Rust boot-marker follow-up mirrored the successful proof's initial BCM2712 `uart10` write before the RP1 pinmux/UART marker path. Local gates passed: Pi 5 image build, disassembly inspection, prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `fb54e604857a59ff9110e3e1be103fd07c80c8860f0a8f98b11b5346683bbee0` published and power-cycled successfully; TFTP served the 83,304-byte `da591740/kernel_2712.img` twice, but serial observed only NUL/newline and no `B0`, `B1`, `B2`, or Talos banner. The pre-run snapshot `pre-rust-uart10-marker-20260520011142` restored the lab to first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`.
- 2026-05-20 linker-layout assembly proof used Talos' normal Pi 5 linker-script section shape while keeping the successful UART proof code: `.text.boot` at `0x80000`, vectors at `0x80800`, `.text` at `0x81000`, `.rodata` at `0x91000`, and an 83,304-byte Image. Local gates passed: shell syntax, proof build, Image header/section inspection, prefixed archive review, mdBook, and `git diff --check`. Hardware archive digest `74c529b6b6127ab316e9c332d898a1d76bacfdbeeeac4875838dfb72e977371c` published and power-cycled successfully; TFTP served the 83,304-byte `da591740/kernel_2712.img` twice and serial printed repeated `TL` after BL31. The pre-run snapshot `pre-uart-linker-layout-20260520012825` restored the lab to first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`. This rules out normal linker section gaps, image padding, and rodata placement as the Rust marker failure cause.
- 2026-05-20 exact-entry Rust marker follow-up replaced the normal Rust image's entry marker with a closer copy of the successful assembly UART sequence and marker `BR`. Local gates passed: Pi 5 image build, disassembly inspection, prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `5a94f900ebf80131790677d52a9c6c27d36d391497e6bd9a5dad7c7cc7b48c74` published and power-cycled successfully; TFTP served the 83,304-byte `da591740/kernel_2712.img` twice, but serial observed only NUL/newline and no `BR` marker or Talos banner. The pre-run snapshot `pre-rust-exact-entry-20260520014106` restored the lab to first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`.
- 2026-05-20 inline-entry Rust marker follow-up first compared the normal Cargo/Rust-linked image against the standalone linker-layout assembly proof: PT_LOAD offsets/load addresses, section start addresses, and final Image size matched, while the Rust `.text.boot` was 336 bytes instead of the proof's 272-byte boot section. The Rust image then inlined the proven UART sequence at `_start` with marker `BI`, before CPACR, BSS, stack, or `rust_entry`. Local gates passed: Pi 5 image build, disassembly inspection, prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `f190d5ff0348c1004c5d56f545745ad37589c541c96047ecae94cc2c6a0e29aa` published and power-cycled successfully; TFTP served the 83,304-byte `da591740/kernel_2712.img` twice, but serial observed only NUL/newline and no `BI` marker or Talos banner. The pre-run snapshot `pre-rust-inline-entry-20260520015636` restored the lab to first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`.
- 2026-05-20 Cargo-linked assembly-only diagnostic added `TALOS_RPI5_CARGO_ASM_UART_PROOF=1`, which keeps the normal Cargo/Rust link path and 83,304-byte Image but makes `_start` write marker `CA` with the successful RP1 readback-flush UART sequence and park before CPACR, BSS, stack, exception setup, or `rust_entry`. Local gates passed: script syntax, proof build, Image header inspection, disassembly showing `CA` before the WFE loop, serial-prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `2ebf5a85c27c662812bc5c5c816372f83318cb288a01e1a9dc2e447f64e2d4f9` published and power-cycled successfully; TFTP cursor `583781->585132` served the 83,304-byte `da591740/kernel_2712.img` twice, but serial advanced `55456->55458` with only NUL/newline and no `CA` marker. The pre-run snapshot `pre-cargo-asm-uart-20260520021339` restored the lab to first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`. This narrowed the follow-up to whether a single early marker was too weak as an observation signal.
- 2026-05-20 Cargo-linked assembly marker loop follow-up changed the same diagnostic to write repeated `CL` markers before CPACR, BSS, stack, exception setup, or `rust_entry`. Local gates passed: proof build, disassembly showing the repeated `CL` loop, serial-prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `33e9fcecfc2797d03c094a56d231f343a2e4b94706165390968a6233f3c2e12b` published and power-cycled successfully; TFTP cursor `585132->586483` served the 83,304-byte `da591740/kernel_2712.img` twice, and serial cursor `55458->120994` captured repeated `CL`/carriage-return-shaped marker output. The pre-run snapshot `pre-cargo-asm-loop-20260520022606` restored the lab to first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`. This proves the Cargo-linked Image can enter and write through the Pi 5 UART; the remaining bring-up boundary is the transition from early assembly marker to BSS/stack/Rust console output.
- 2026-05-20 transition diagnostic added `TALOS_RPI5_TRANSITION_DIAGNOSTIC=1`, plus `scripts/rpi5-transition-diagnostic-image.sh` and `scripts/rpi5-transition-diagnostic-tree.sh`. The image keeps the normal Cargo/Rust link and stages repeated markers: `S0` before RP1 pinmux, `S1` after pinmux, `S2` after CPACR, `S3` after BSS clearing, and `S4` after stack setup before `rust_entry`. Local gates passed: proof build/disassembly, serial-prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`.
- 2026-05-20 transition diagnostic hardware attempts localized a diagnostic issue before `S1`. First archive digest `8f71f02d82a1b4ff6feac1b64a535f69217b4ea396e337642d19e1f7f6ac0988` served the 83,304-byte image twice, but serial showed repeated `S0` only; review found the nested `rpi5_write_stage` helper did not preserve LR and was looping in `S0`. After fixing LR preservation, archive digest `72b4c99c8ffbde817c0752cd80ac53554a7115954eb5c2c118dbfa2ea38af442` again served the image twice and still showed `S0` only. A third version made `S0` UART10-only before RP1 pinmux; archive digest `8a80cf578e8fc0b470e7b1067fbebebe1ab7288efdb939a10b1c6512d345985f` served the image twice and serial showed firmware logs plus repeated `S0`, with no `S1`-`S4` or Talos banner. Each run restored the lab to first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`. The next smallest step is to align the staged diagnostic with the successful `CL` proof shape: one pre-pinmux UART10 marker, then pinmux, then a repeated post-pinmux marker, before adding CPACR/BSS/stack stages.
- 2026-05-20 simplified transition diagnostic changed `TALOS_RPI5_TRANSITION_DIAGNOSTIC=1` to emit one pre-pinmux `S0` marker, apply the RP1 pinmux/readback flush sequence, emit repeated `S1` markers, and park before CPACR, BSS, stack, or Rust. Local gates passed: transition image build/disassembly, serial-prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `4db509ee4e4364cf3778ba5fd1eb0da5d8240d57cc3a502ec151ec7d0f0eab64` published and power-cycled successfully; TFTP cursor `590536->591887` served the 83,304-byte `da591740/kernel_2712.img` twice. Fresh serial cursor `187177->187179` captured only NUL plus a partial `S`, with no complete `S0`, `S1`, or Talos banner before restoring snapshot `pre-transition-s1-loop-20260520025710` to the accepted first-light proof tree. The next diagnostic should reuse the exact successful `rpi5_write_marker` literal/helper shape around the pinmux boundary to separate a helper-shape issue from an actual pinmux transition issue.
- 2026-05-20 exact-marker transition diagnostic reused the successful `rpi5_write_marker`/literal shape with marker `CT`: one write before RP1 pinmux, one write after pinmux, then a repeated UART10/RP1 loop before CPACR, BSS, stack, or Rust. Local gates passed: transition image build/disassembly, serial-prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `76f94d54bab20509f5e32d946a117169fb1b1a656e2f4770647c365110565ba6` published and power-cycled successfully; TFTP cursor `591887->593238` served the 83,304-byte `da591740/kernel_2712.img` twice, and serial cursor `187179->252715` captured repeated `CT`-shaped output. Snapshot `pre-transition-exact-marker-20260520031145` restored the lab to the accepted first-light proof tree. This shows the RP1 pinmux boundary is still good when the proven marker helper/literal shape is preserved; the failed `S0`/`S1` variants were diagnostic-shape failures.
- 2026-05-20 CPACR transition diagnostic kept the proven marker shape and moved the repeated marker loop after CPACR_EL1 FP/SIMD enable, with marker `CP`, still before BSS, stack, or Rust. Local gates passed: transition image build/disassembly, serial-prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `9cb273065ab07bbedc87d243b9249575ca390fa91860a37f9b67385a954b11e4` published and power-cycled successfully; TFTP cursor `593238->594589` served the 83,304-byte `da591740/kernel_2712.img` twice, and serial cursor `252715->318251` captured repeated `CP`-shaped output. Snapshot `pre-transition-cpacr-20260520032601` restored the lab to the accepted first-light proof tree. This clears CPACR as a failure boundary; next transition should add BSS clearing while preserving the proven marker helper/literal shape.
- 2026-05-20 BSS transition diagnostic kept the proven marker shape and moved the repeated marker loop after BSS clearing, with marker `BS`, still before stack or Rust. Local gates passed: transition image build/disassembly, serial-prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `c6f77b6fd8cd797763d0fdcaef08792896bd175c6d73e8d25775a1b67bcfddcc` published and power-cycled successfully; TFTP cursor `594589->595940` served the 83,304-byte `da591740/kernel_2712.img` twice, and serial cursor `318251->383787` captured repeated `BS`-shaped output. Snapshot `pre-transition-bss-20260520034100` restored the lab to the accepted first-light proof tree. This clears BSS zeroing as a failure boundary; next transition should add stack pointer setup while preserving the proven marker helper/literal shape.
- 2026-05-20 stack transition diagnostic kept the proven marker shape and moved the repeated marker loop after stack pointer setup, with marker `SP`, still before Rust entry. Local gates passed: transition image build/disassembly, serial-prefixed archive review, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive digest `adf0e56db7d458bf9487464e88f4f794b5422aea2c085e79dc692b8c2c2a652d` published and power-cycled successfully; TFTP cursor `595940->597291` served the 83,304-byte `da591740/kernel_2712.img` twice, and serial cursor `383787->449323` captured repeated `SP`-shaped output. Snapshot `pre-transition-stack-20260520035600` restored the lab to the accepted first-light proof tree. This clears stack pointer setup as a failure boundary; next transition should call into `rust_entry` or insert a marker immediately at Rust entry while preserving the proven marker helper/literal shape.
- 2026-05-20 Rust-entry diagnostic added `TALOS_RPI5_RUST_ENTRY_DIAGNOSTIC=1` plus staging scripts to emit a repeated marker immediately inside `rust_entry` after the proven assembly stack setup. The first version used Rust helper functions/slices and marker `RE`; the second version used a single inline-assembly `rust_entry` loop with marker `RI`. Local gates passed for both versions: diagnostic image/archive review, disassembly inspection, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archives `03e627b16304c6598299e84fd5e537481c30b8805103c5e21adabae1887fcb7e` and `004279adba43420774a07ba6f4baf68da27d2f6b36a73fad4f46a312c254a2bb` each published and power-cycled successfully; the 70,288-byte and 69,728-byte kernels were served repeatedly, but serial advanced only by NUL/newline with no `RE`, `RI`, or Talos banner. Snapshots `pre-rust-entry-marker-20260520041318` and `pre-rust-entry-inline-20260520041529` restored the accepted first-light proof tree after the tests.
- 2026-05-20 text-section diagnostic added `TALOS_RPI5_TEXT_SECTION_DIAGNOSTIC=1` plus staging scripts to branch from `.text.boot` after stack setup into an assembly marker loop placed in normal `.text`. The first version linked the probe at `0x90a30`; the follow-up pinned `.text.rpi5_text_section_diagnostic` to the start of the `.text` output section at `0x81000`. Local gates passed: diagnostic image/archive review, disassembly inspection, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archives `f760c365db0d18a2dfeb54700897bd9600d0bd61ddc24042d67dd75b75193a34` and `c048e51f54177485db27256ac73da0de256f119b0b36f49a397f50ec3002b0c8` each published and power-cycled successfully with 83,304-byte kernels, but serial again advanced only by NUL/newline with no `TX` marker. Snapshots `pre-text-section-marker-20260520041721` and `pre-text-section-start-20260520041841` restored the accepted first-light proof tree. This suggests the next useful review is the branch/load/execution contract between `.text.boot` and later sections, not another UART or stack setup variant.
- 2026-05-20 post-`.text` review tested two narrower branch/load hypotheses. First, adding `ic iallu; dsb sy; isb` before the `.text` branch still produced no `TX` marker; archive `1caa338cf1745489dfa748be726ef5ead3cbd7363640d33339c7d7daa97b79a9` published and restored around snapshot `pre-text-section-icache-20260520042631`, with serial only NUL/newline. The I-cache maintenance was removed from the committed path because it did not improve the diagnostic and may not be safe at the unknown firmware handoff EL.
- 2026-05-20 vector-section diagnostic added `TALOS_RPI5_VECTOR_SECTION_DIAGNOSTIC=1` plus staging scripts to branch from `.text.boot` to a marker loop placed at the start of the `.vectors` output section. This tests whether execution can leave `.text.boot` within the first 4 KiB before trying normal `.text` or Rust. Local gates passed: diagnostic image/archive review, disassembly inspection showing `bl 0x80800 <rpi5_vector_section_marker_loop>`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. A first run with the temporary I-cache maintenance and a second run without it both published and restored cleanly, but neither printed `VX`; archives `0dafe0e1d9d80f34857045775b81c8466faf261df8a90f3f763e5da8597a552b` and `82c228f88053cca1cb09f4edecaa0fd31f5e443470af824cab5ef930b5017a61` produced only NUL/newline on serial. Snapshots `pre-vector-section-marker-20260520042914` and `pre-vector-section-noic-20260520043036` restored the accepted first-light proof tree. The current failure boundary is now specifically leaving the `.text.boot` section after stack setup, even to `0x80800`; next work should inspect linker/script placement and create a same-`.text.boot` far-branch control before another later-section test.
- 2026-05-20 same-`.text.boot` branch diagnostic added `TALOS_RPI5_TEXT_BOOT_FAR_DIAGNOSTIC=1` plus staging scripts to branch from the proven post-stack setup point to a repeated `BF` marker loop still inside `.text.boot`. Local gates passed for both placements: diagnostic image/archive review, disassembly inspection, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive `ac73f4a006552a05a124580675ad77ce71631b1b0c30fbfa9d49b0beb8ccb264` placed the loop at `0x80400`; archive `a6a51e7cb0fa2945591d2b5e1ec7bf6f29a102bdb157bef3e9bea93fa66eddd0` placed the loop at `0x80200`. Both 83,304-byte kernels published and power-cycled successfully, but serial advanced only by NUL/newline with no `BF` marker. Snapshots `pre-text-boot-far-20260520044116` and `pre-text-boot-mid-20260520044401` restored the accepted first-light proof tree. This narrows the failure from “leaving `.text.boot`” to branching beyond the immediate proven boot code range after stack setup; next work should test a very-near same-section branch/control-flow shape before trying another section or Rust-entry variant.
- 2026-05-20 very-near `.text.boot` control-flow diagnostics tested the smallest branch shapes after the proven stack-setup point. `TALOS_RPI5_TEXT_BOOT_NEAR_DIAGNOSTIC=1` used `bl 0x80110` to a same-section `BN` marker loop; `TALOS_RPI5_TEXT_BOOT_NEAR_BRANCH_DIAGNOSTIC=1` used `b 0x80110` to the same shape with marker `BB`; `TALOS_RPI5_TEXT_BOOT_NEXT_BRANCH_DIAGNOSTIC=1` used `b 0x800d8` to an adjacent inline marker loop with marker `NB`. Local gates passed for all three, including archive review, disassembly inspection, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archives `da8ef2646a914b6138c9c2c5721a82db68f2ebd233487f2f7f7f57aa2183311d`, `9bfbbf4dbf9ba595fa47efdc20ec03f92f88c116eaf39cee2c7d8d87c8a59003`, and `85282466f588f92cb03f427813634fa2aeb6602d21c1a6d1754d333cfaa52370` each published 83,304-byte kernels and power-cycled successfully, but serial advanced only by NUL/newline with no marker. Snapshots `pre-text-boot-near-20260520045648`, `pre-text-boot-near-branch-20260520045857`, and `pre-text-boot-next-branch-20260520050047` restored the accepted first-light proof tree. Since the earlier post-stack `SP` diagnostic printed when it fell directly into the marker loop, the next useful review should compare the successful fallthrough layout against these explicit-branch layouts and avoid adding later-section/Rust variants until that control-flow discrepancy is explained.
- 2026-05-20 follow-up post-stack fallthrough controls compared against the successful `SP` layout. The successful `SP` diagnostic had `mov sp, x0` followed immediately by the first marker-loop `ldr x9, ...` at `0x800d4`. `TALOS_RPI5_FALLTHROUGH_RUST_DIAGNOSTIC=1` inserted `mov x20, #0x4000`, emitted finite repeated `FR` markers, then called `rust_entry`; `TALOS_RPI5_POST_STACK_NOP_DIAGNOSTIC=1` inserted a single `nop` before an infinite `PN` marker loop. Local gates passed for both, including archive review, disassembly inspection, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archives `58de6ffccd6de15cd29dd0e46f1a5083f5ef504b7c405a01eaff99b65f7dfff2` and `51b974f8bc0b4594b423752257d1071d13b55ab7b014d04f2cc1790d05e8310c` each published 83,304-byte kernels and power-cycled successfully, but serial advanced only by NUL/newline with no marker. Snapshots `pre-fallthrough-rust-20260520051133` and `pre-post-stack-nop-20260520051318` restored the accepted first-light proof tree. The current boundary is now extremely narrow: after stack setup, the exact immediate fallthrough into the proven marker loop prints, while adding even one instruction or branch before that first marker write does not.
- 2026-05-20 serial evidence review found the high-volume marker-loop capture path can be stale or misleading. Re-running the current `SP` transition diagnostic built archive `8bcd1ff024e277ac4a143b9bfe4459c082ad162926f0842f75bdb5a09a472b83` and initially `/serial/observe` returned only NUL/newline, but a later `/serial/peek` showed a large `SP`-shaped backlog. Attempts to drain with `/serial/peek?drain=true&max_bytes=65536` repeatedly returned the same cursor and same sample, so the cursor/drain contract is unreliable after high-volume marker output. A clean `PN` follow-up could not be classified because the serial sample remained dominated by stale `SP` output. The current post-stack no-marker results should therefore be treated as serial-inconclusive unless backed by a bounded fresh sample or a non-serial side effect. Next diagnostics should avoid infinite high-volume loops and use bounded distinctive markers plus a second observable side effect.
- 2026-05-20 bounded post-stack reset diagnostic added `TALOS_RPI5_POST_STACK_RESET_DIAGNOSTIC=1` plus staging scripts. The diagnostic preserves the normal Cargo/Rust-linked 83,304-byte image, reaches the post-stack point, emits eight bounded `RS` marker bursts through the proven UART helper shape, then calls PSCI `SYSTEM_RESET` in a loop. Local gates passed: archive review, disassembly inspection showing the eight-count marker loop and `smc #0` with `0x84000009`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive `763f0563f8b16cdf08b711307742d4fa40f71e073b5dbf10fb7a42fe3c79566a` published and power-cycled successfully under snapshot `pre-post-stack-reset-20260520054128`; TFTP cursor `621609->622960` captured 13 fresh events including two served `da591740/kernel_2712.img` events at 83,304 bytes. Serial remained dominated by stale `SP` backlog. The archive was restored to the accepted first-light proof tree. This proves the new bounded diagnostic is staged and fetched, but the first observation window did not capture a reset-induced second boot sequence beyond the normal duplicate kernel fetches, so the post-stack reset side effect remains unproven; next run should wait for a delayed TFTP delta before restore rather than restoring as soon as the first TFTP activity appears.
- 2026-05-20 delayed post-stack reset diagnostic re-ran the same 83,304-byte `RS` archive `763f0563f8b16cdf08b711307742d4fa40f71e073b5dbf10fb7a42fe3c79566a` with kernel digest `c631f762ed1163196433654ba12cd42b1b061b5ec2a91450462fe3593ae1c02e` under snapshot `pre-post-stack-reset-delayed-20260520055534`. The initial TFTP window `624311->625662` captured the normal first boot. After delaying restore, the second TFTP window `625662->628364` captured 26 fresh events, including four served 83,304-byte `da591740/kernel_2712.img` events at 05:56:15 and 05:56:32/05:56:33 UTC. Serial remained stale/inconclusive, but the delayed repeated boot/fetch sequence is the required non-serial side effect proving the post-stack `RS` marker loop reached PSCI reset. Snapshot restore then returned the lab to accepted first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`. This accepts the post-stack assembly boundary at serial-hardware/TFTP-side-effect level; the next diagnostic should cross into or immediately after `rust_entry` with a bounded marker plus reset side effect, rather than depending on high-volume serial output.
- 2026-05-20 Rust-entry reset-side-effect diagnostic changed `TALOS_RPI5_RUST_ENTRY_DIAGNOSTIC=1` to emit eight bounded inline-assembly `RI` marker bursts at the start of `rust_entry`, then call PSCI `SYSTEM_RESET` in a loop. Local gates passed: serial-prefixed archive review, disassembly inspection showing the bounded loop and `smc #0` with `0x84000009`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. Hardware archive `c96f10cd4dad5c382804b079c1b78300e659bd406cb806e3a772b118738ca964` with kernel `b6d4ef09bc6e1d20683d535847ee19fb3150d60eec8605184a12c96bc5d4e44e` / 69,728 bytes published and power-cycled successfully under snapshot `pre-rpi5-rust-entry-reset-20260520061332`. Initial TFTP cursor `629715->631066` served the diagnostic kernel twice. After snapshot restore, cursor `629715->632417` showed two additional accepted first-light proof boot sequences serving the restored 272-byte kernel without another power-cycle, proving the Rust-entry diagnostic reached PSCI reset even though serial remained stale/inconclusive. The lab was restored to accepted first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`. This accepts the Rust-entry boundary at TFTP-side-effect level; the next useful diagnostic should move the reset side effect after the first real Rust initialization step rather than staying inside the inline entry marker block.
- 2026-05-20 BootInfo reset-side-effect diagnostic moved the reset probe after `BootInfo::from_aarch64_x0`, with bounded inline-assembly `RB` marker bursts followed by PSCI `SYSTEM_RESET`. Local gates passed: archive review, disassembly showing `rust_entry` calls `BootInfo::from_aarch64_x0` before the reset probe, bounded `RB` marker writes and `smc #0`, `cargo fmt --check`, `cargo -Zjson-target-spec test`, QEMU smoke, mdBook, and `git diff --check`. The clean hardware run published archive `1c7bc710783ddfc36ea53f808639d0a152d108c9129ba07eae6871e42825992d` with kernel `38461bc110e2acaadedc58e67f3bda9146c5c04da39410b9bd62b42125edc1e3` / 69,728 bytes under snapshot `pre-rpi5-rust-bootinfo-reset-clean-20260520062828`; TFTP cursor `635119->640523` captured 52 fresh events, including four repeated boot/fetch sequences and eight served diagnostic `da591740/kernel_2712.img` events before restore. Snapshot restore returned the lab to accepted first-light proof tree hash `da58994fc0492a8cfee9c7b081c49a4cd0a15f7ed97a0508e0035a9671d5e102`. This accepts `BootInfo` construction at TFTP-side-effect hardware level; the next diagnostic should move the bounded reset side effect after `target::init` or exception-vector setup.
