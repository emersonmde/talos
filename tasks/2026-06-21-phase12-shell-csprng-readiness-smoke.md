# Phase 12.5 shell CSPRNG readiness smoke

Task id: phase12-shell-csprng-readiness-smoke-20260621

Status: accepted.

Classification: phase12-shell-csprng-readiness-smoke-accepted.

## Goal

Retain host/QEMU-substitute shell/internal smoke evidence that the accepted
operator-seeded CSPRNG readiness boundary can drive metadata-only entropy and
sshkeydiag readiness without exposing seed bytes, generated bytes, digests,
fingerprints, stream identifiers, secret material, SSH service behavior,
hardware reachability, or a phase transition.

## Scope

- Added a task-owned retained smoke script for the accepted CSPRNG readiness,
  entropy metadata, and SSH key-readiness classifier path.
- Exercised missing, insufficient, and sufficient public fixture seed cases.
- Retained a transcript under task evidence.
- Kept the smoke at host/QEMU-substitute test level; no Pi 5 hardware,
  boot-archive publication, or live transport action is part of this task.

## Non-goals

- No source behavior changes.
- No host-key generation/provisioning, authorized-key storage, writable seed
  persistence, SSH service behavior, live transport, hardware/lab action,
  hardware reachability, public ABI/POSIX/Linux compatibility, stale link-ready
  discriminator promotion, broad expansion, or phase transition.
- No real operator secret, generated random byte stream, digest, fingerprint,
  stream identifier, RNG state, serialized state, or comparable secret
  identifier retained in evidence.

## Findings

- fixed: added scripts/qemu-shell-csprng-readiness-smoke.sh as the retained
  host/QEMU-substitute smoke gate for the CSPRNG readiness metadata path.
- fixed: retained
  tasks/evidence/2026-06-21-shell-csprng-readiness-smoke/qemu-shell-csprng-readiness-smoke.log
  with missing, insufficient, and sufficient public fixture case summaries.
- not-an-issue: the retained smoke transcript records labels and validation
  commands only; it does not include seed bytes, generated bytes, digests,
  fingerprints, stream identifiers, RNG state, serialized state, or comparable
  secret identifiers.
- deferred: host-key generation/provisioning, authorized-key storage, writable
  seed persistence, SSH service behavior, live transport, hardware reachability,
  public ABI/POSIX/Linux compatibility, broad expansion, stale link-ready
  discriminator work, and phase transition remain future work.

## Smoke Evidence

Script:

    scripts/qemu-shell-csprng-readiness-smoke.sh

Retained transcript:

    tasks/evidence/2026-06-21-shell-csprng-readiness-smoke/qemu-shell-csprng-readiness-smoke.log

The retained transcript records:

    qemu-shell-csprng-readiness-smoke: boundary=internal csprng readiness plus entropy and sshkeydiag metadata classifiers
    qemu-shell-csprng-readiness-smoke: missing-seed=csprng-missing-seed,entropydiag-operator-seed-required,sshkeydiag-seed-material-missing,cryptographic-strength=false,ssh-ready=false
    qemu-shell-csprng-readiness-smoke: insufficient-seed=csprng-insufficient-seed,sshkeydiag-seed-material-insufficient,cryptographic-strength=false,ssh-ready=false
    qemu-shell-csprng-readiness-smoke: sufficient-public-fixture=csprng-ready,cryptographic-strength=true,sshkeydiag-not-ready,ssh-ready=false
    qemu-shell-csprng-readiness-smoke: redaction=no-seed-bytes,no-generated-bytes,no-digest,no-fingerprint,no-stream-identifier,no-secret-material
    qemu-shell-csprng-readiness-smoke: PASS classification=host-qemu-substitute-shell-csprng-readiness-smoke-complete

The smoke gate runs:

- missing_invalid_and_insufficient_operator_seeds_fail_closed
- not_ready_fill_zeroizes_caller_buffer_without_partial_output
- public_fixture_seed_initializes_bounded_deterministic_output
- ready_csprng_metadata_sets_crypto_strength_without_ssh_readiness
- csprng_ready_entropy_clears_only_entropy_prerequisite

The underlying no_std test harness reported all tests passing in the retained
transcript.

## Validation

- scripts/qemu-shell-csprng-readiness-smoke.sh: pass.
- cargo fmt --all -- --check: pass.
- git diff --check: pass.
- static redaction review: pass; retained smoke transcript contains no seed
  bytes, generated byte streams, digest, fingerprint, stream identifier, real
  operator secret, RNG state, serialized state, or comparable secret identifier.
- /home/node/.cargo/bin/mdbook build: pass; existing warning that the search
  index is very large.
- git diff --cached --check: pass.

## Acceptance

- accepted: retained host/QEMU-substitute smoke evidence covers missing,
  insufficient, and sufficient public fixture operator seed cases through the
  accepted CSPRNG readiness, entropy, and SSH key-readiness metadata path.
- accepted: sufficient public fixture seed evidence demonstrates only
  cryptographic-strength metadata transition; ssh-ready remains false.
- accepted: negative cases fail closed and preserve existing not-ready labels.
- accepted: retained transcript contains no seed bytes, generated bytes, digests,
  fingerprints, stream identifiers, RNG state, serialized state, real operator
  secret, or comparable secret identifier.

selected_next_task=phase12-operator-seeded-csprng-closeout-20260621.
