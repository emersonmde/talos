# Phase 12.6 SSH KEXINIT negotiation smoke

Task id: phase12-shell-ssh-kexinit-negotiation-smoke-20260622
Status: accepted
Owner: worker
Classification: phase12-shell-ssh-kexinit-negotiation-smoke-accepted.

## Goal

Retain host/QEMU-substitute smoke evidence that the accepted local modeled
KEXINIT/algorithm-negotiation diagnostic surface reports the accepted positive,
negative, and fail-closed states without accepting actual key exchange,
encryption, NEWKEYS, host-key signing, authentication, sessions, shell
attachment, hardware reachability, public compatibility, broad expansion, or a
phase transition.

## Scope

- Added task-owned smoke harness
  scripts/qemu-shell-ssh-kexinit-negotiation-smoke.sh.
- Retained transcript evidence under
  tasks/evidence/2026-06-22-ssh-kexinit-negotiation-smoke/.
- Covered the accepted source/QEMU-substitute KEXINIT paths:
  - fixed-policy negotiated state with cookie-generation redaction;
  - first_kex_packet_follows=true ignored while staying fail-closed;
  - unsupported algorithm classification without retained client text;
  - malformed packet, packet-over-limit, and list-over-limit classifications;
  - disabled and prerequisite-missing states with zero listener/transport
    counters.

## Findings

- fixed: Retained smoke coverage now records the accepted modeled KEXINIT
  negotiated state through the QEMU-substitute source tests.
- fixed: Retained smoke coverage now records unsupported algorithm, malformed
  packet, oversized packet, oversized list, first-packet-follows, disabled, and
  prerequisite-missing negative paths.
- not-an-issue: The smoke transcript records public fixed Talos algorithm-policy
  labels. It does not retain generated bytes, KEX cookie bytes, client packet
  bytes, client algorithm-list text, key material, peer text/address, digests,
  fingerprints, operator identity, key-derived identifiers, or stable
  transport/session identifiers.
- deferred: Actual X25519/ECDH, Ed25519 host-key parsing/signing, packet
  encryption/MAC, NEWKEYS, authentication, session/channel handling, PTY
  allocation, shell attachment, hardware reachability, and OpenSSH/POSIX/Linux
  compatibility remain separate future work.

## Evidence

- host/QEMU-substitute smoke transcript:
  tasks/evidence/2026-06-22-ssh-kexinit-negotiation-smoke/qemu-shell-ssh-kexinit-negotiation-smoke.log
- The transcript records:
  - negotiated state: sshservicediag-kexinit-modeled,
    sshservicediag-kexinit-cookie-generated-redacted,
    sshservicediag-kexinit-client-packet-valid,
    sshservicediag-kexinit-algorithm-negotiated,
    sshservicediag-kexinit-selected-kex-curve25519-sha256,
    sshservicediag-kexinit-selected-hostkey-ssh-ed25519,
    sshservicediag-kexinit-selected-cipher-chacha20-poly1305-openssh,
    sshservicediag-kexinit-selected-mac-hmac-sha2-256,
    sshservicediag-kexinit-selected-compression-none, and ssh-ready=false.
  - negative states: unsupported-algorithm, malformed-packet,
    packet-over-limit, list-over-limit, and first-packet-follows-ignored.
  - disabled/prerequisite states: zero listener/connection counters,
    transport-enabled=false, and ssh-ready=false.

## Redaction Review

Retained evidence contains fixed labels, counters, booleans, test names,
non-goal labels, and validation output only. It does not retain generated
random bytes, KEX cookie bytes, client packet bytes, client algorithm-list
text, private host-key bytes, authorized-key bytes, shared secrets,
signatures, fingerprints, digests, peer addresses, peer identification text,
operator identity, key-derived identifiers, or stable transport/session
identifiers.

## Validation

- scripts/qemu-shell-ssh-kexinit-negotiation-smoke.sh: pass.
- cargo fmt --all -- --check: pass.
- cargo -Zjson-target-spec test --quiet sshservicediag: pass
  (QEMU/substitute no_std runner; 750 tests passed).
- cargo -Zjson-target-spec test --quiet: not run; Rust source was not touched.
- jq empty on task-owned JSON evidence: not applicable; no task-owned JSON
  evidence created.
- git diff --check: pass.
- /home/node/.cargo/bin/mdbook build: pass.
- git diff --cached --check: pass.

Validation levels: task-owned host/QEMU-substitute smoke transcript,
fmt/lint/typecheck, unit tests under QEMU/substitute, docs build, and diff
checks. No Pi 5 hardware run, lab-controller API action, hardwareTestLock
acquisition, boot publication, generated-root publication, live packet I/O,
hardware reachability, runtime SSH crypto, authentication/session work, shell
attachment, OpenSSH/POSIX/Linux compatibility claim, broad expansion, or phase
transition was performed.

## Acceptance

Accepted. The retained host/QEMU-substitute smoke evidence covers the accepted
local modeled KEXINIT/algorithm-negotiation positive, negative, and fail-closed
states while keeping ssh-ready false. No actual key exchange, encryption/MAC,
NEWKEYS, host-key signing, authentication/session/shell behavior, hardware
reachability, public compatibility, broad expansion, or phase transition is
accepted.

selected_next_task=phase12-ssh-kexinit-negotiation-closeout-20260622.
