# Phase 10 Pi 5 Accepted Control Entry Provenance Proof

Task: phase10-pi5-accepted-control-entry-provenance-proof-20260602
Status: accepted-blocked-firmware-fetch-no-entry-marker-control
Commit: b52e94d3a85de209484a076ac3db4d075555f15e

## Goal

Run the accepted literal-echo Pi 5 control archive with the new earliest
entry-provenance markers on serialized hardware, so the cd blocker can be
compared against a prompt-capable control that should reach the same assembly
entry path.

## Selected Control

- Control scenario: rpi5_local_literal_echo.
- Prior accepted proof: phase10-pi5-local-literal-echo-proof-20260531.
- Prior accepted commit: 29b8b7d6afbc57dc156db593c376aef36640ebb1.
- Entry-provenance core commit:
  1fb327882bc98c3147eb264092f58d1991105696.
- Published archive:
  target/talos-rpi5-local-literal-echo-entry-provenance-control.tar.gz.
- Archive sha256:
  9580caf5c18139ca413a59adf717cdab4401b8452cdbfba7c0b3226e85f76694.
- Kernel sha256:
  ee1128ac076b48e24f926e5f91e75f02d75c87adadea4e562b4f6040cb504a52.
- Archive review: kernel_2712.img size 108896 bytes, arm64 Image header
  size 108896, text_offset 0, flags 12.

## Run

Accepted evidence is retained under:

tasks/evidence/2026-06-02-pi5-accepted-control-entry-provenance-proof/local2-literal-echo-entry-provenance-control/.

The worker held hardwareTestLock for the serialized proof and restored the
named pre-run boot snapshot before releasing it. Pre-run and post-restore boot
tree hash both matched:

`a0452458391d0e398b7e17e0f068bb652235f666bf277d004e0e214626128d10`.

The local2 run captured a fresh serial cursor before power cycle, published
only the selected literal-echo entry-provenance control archive, then collected
a settled same-cursor TFTP delta before restore. The TFTP delta retained 13
events from 10.42.1.4, including two fresh
`da591740/kernel_2712.img` serves of the expected 108896-byte control kernel.

Serial was observed from the fresh pre-run cursor only after the settled TFTP
window. The fresh serial artifact retained two bytes, NUL and space. It did
not retain `TALOS: asm_start`, `TALOS: asm_pre_rust_entry`, `TALOS:
rust_entry`, the literal-echo proof start/ready lines, a `talos>` prompt,
literal echo output, final classification, or
`rpi5-local-literal-echo-proof: PASS`. No command was written because no fresh
prompt was observed.

The earlier local1 attempt is retained only as an invalid setup/evidence
collection attempt. It used a wrong tar member path while deriving the kernel
hash and was manually restored before a serial observe/classification could be
completed. Local2 supersedes it for task classification.

## Classification

Final classification: firmware-fetch-no-entry-marker-control.

The lab proved fresh network boot of the selected entry-provenance control
archive, but the fresh serial stream did not retain the earliest assembly entry
markers. This means the current Pi 5 entry-provenance blocker is not specific
to the cd fixed-directories proof archive. The original cd proof remains
blocked, and the queued cd entry-regression core is not mechanically unblocked
because the control proof did not classify as prompt-entry-responsive.

## Evidence

- control identity, archive review, hashes, lab status, snapshot, publish,
  power-cycle, TFTP, serial, restore, and classification:
  tasks/evidence/2026-06-02-pi5-accepted-control-entry-provenance-proof/local2-literal-echo-entry-provenance-control/.
- classification summary:
  tasks/evidence/2026-06-02-pi5-accepted-control-entry-provenance-proof/local2-literal-echo-entry-provenance-control/classification-summary.json.
- settled TFTP delta:
  tasks/evidence/2026-06-02-pi5-accepted-control-entry-provenance-proof/local2-literal-echo-entry-provenance-control/tftp-delta-settled-before-restore.json.
- fresh serial observe:
  tasks/evidence/2026-06-02-pi5-accepted-control-entry-provenance-proof/local2-literal-echo-entry-provenance-control/serial-observe-post-tftp.json.
- restore proof:
  tasks/evidence/2026-06-02-pi5-accepted-control-entry-provenance-proof/local2-literal-echo-entry-provenance-control/restore-proof.txt.

## Validation

- pre-run static state: git status was clean except task evidence generated by
  this task.
- image/archive inspection: scripts/rpi5-archive-review.sh passed for the
  selected control archive, and archive string inspection confirmed the
  entry-provenance marker strings were present in the archive kernel image.
- lab-controller API: health, status, boot files, named snapshot, publish,
  fixed-port power-cycle, TFTP logs, serial observe/peek, named snapshot
  restore, and post-restore status artifacts are retained.
- TFTP/lab-controller API: settled same-cursor delta before restore retained
  two 108896-byte kernel_2712.img serves for the selected control archive.
- serial hardware boot/output: post-TFTP serial observe from the captured
  cursor retained only NUL/space and no entry marker, Rust marker, prompt,
  literal-echo transcript, classification, or PASS.
- restore proof: post-restore status returned the lab to the pre-run tree hash,
  and hardwareTestLock was released/restored after the run.
- static inspection: git diff --check passed after evidence/doc updates.

## Next Action

Supervisor planning is required. Do not promote the cd entry-regression core
or any cd recovery proof from this task, because the control proof did not
establish prompt-entry-responsive behavior.
