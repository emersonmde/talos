# Phase 12.5 operator-seeded CSPRNG closeout

Task id: phase12-operator-seeded-csprng-closeout-20260621

Status: accepted.

Classification: phase12-operator-seeded-csprng-closeout-accepted.

## Goal

Close out the operator-seeded CSPRNG readiness slice by reconciling the
accepted secret-material contract, dependency-selection contract, source/unit
implementation, retained smoke evidence, docs, deferred work, and rejected
claims.

## Scope

- Reviewed the accepted operator seed secret-material contract, CSPRNG
  dependency selection, operator-seeded CSPRNG core, retained shell/internal
  smoke evidence, Phase 12 architecture notes, roadmap notes, and ADR index.
- Recorded the accepted frontier: Talos can initialize a private no_std
  CSPRNG from sufficient read-only operator seed material and expose only
  metadata that marks cryptographic-strength true, while ssh-ready remains
  false.
- Recorded deferred prerequisites and rejected claims.
- Recorded planningNeeded=true because no later Phase 12.5 prerequisite task is
  already queued with explicit objective dependencies, gates, scope, and
  acceptance criteria.

## Findings

- fixed: the accepted slice now has a closeout record that ties together
  secret-material access, dependency/API selection, CSPRNG core behavior, and
  retained smoke evidence.
- fixed: docs now state that the accepted frontier is internal
  operator-seeded CSPRNG readiness plus metadata-only cryptographic-strength
  reporting; SSH readiness remains rejected until host-key, authorized-key,
  persistence/exposure, service, and reachability prerequisites are accepted.
- fixed: the closeout explicitly preserves redaction boundaries: no real
  operator secret, generated random byte stream, digest, fingerprint, RNG
  state, serialized state, stream identifier, or comparable secret identifier
  is retained in evidence.
- deferred: host-key generation/provisioning policy, authorized-key storage,
  writable seed persistence, SSH service behavior, live transport, hardware
  reachability, public ABI/POSIX/Linux compatibility, stale link-ready
  discriminator work, broad expansion, and phase transition.
- not-an-issue: no source behavior change, JSON evidence, Pi 5 hardware run,
  boot archive publication, hardware lock, or external action is required for
  this closeout-only task.

## Evidence Reviewed

- tasks/2026-06-21-phase12-operator-seed-secret-material-contract.md
- tasks/2026-06-21-phase12-csprng-dependency-selection-contract.md
- tasks/2026-06-21-phase12-operator-seeded-csprng-core.md
- tasks/2026-06-21-phase12-shell-csprng-readiness-smoke.md
- tasks/evidence/2026-06-21-shell-csprng-readiness-smoke/qemu-shell-csprng-readiness-smoke.log
- docs/src/project/phase12-networking-ssh.md
- docs/src/roadmap.md
- docs/src/decisions/README.md

The retained smoke transcript records missing, insufficient, and sufficient
public fixture seed cases only by labels and readiness metadata. The sufficient
case records csprng-ready and cryptographic-strength true while ssh-ready
remains false. The transcript includes no seed bytes, generated bytes, digests,
fingerprints, stream identifiers, RNG state, serialized state, real operator
secret, or comparable secret identifier.

## Validation

- static task/docs/evidence review: pass.
- jq empty on task-owned JSON evidence: not applicable; no task-owned JSON
  evidence was created.
- git diff --check: pass.
- /home/node/.cargo/bin/mdbook build: pass; existing large-search-index warning.
- git diff --cached --check: pass.

## Acceptance

- accepted: the operator-seeded CSPRNG readiness slice is closed with secret
  material, dependency/API, source/unit, retained smoke, docs, deferred work,
  and risk evidence reconciled.
- accepted: Talos can internally initialize a private CSPRNG from sufficient
  read-only operator seed material and report only the accepted
  cryptographic-strength metadata transition.
- accepted: ssh-ready remains false; host-key generation/provisioning,
  authorized-key storage, writable persistence, SSH service, live transport,
  hardware reachability, public ABI/POSIX/Linux compatibility, stale link-ready
  discriminator work, broad expansion, and phase transition remain rejected.
- accepted: no real operator secret, generated random byte stream, digest,
  fingerprint, RNG state, serialized state, stream identifier, or comparable
  secret identifier is retained in evidence.

selected_next_task=null.

planningNeeded=true because no later queued Phase 12.5 prerequisite task has
complete objective dependencies, scope, non-goals, acceptance criteria,
validation gates, docs requirements, and evidence requirements. Supervisor
planning is required before host-key generation/provisioning policy,
authorized-key storage, writable seed persistence, SSH service behavior, live
transport, hardware reachability, public ABI/POSIX/Linux compatibility, stale
link-ready discriminator work, broad expansion, or phase transition.
