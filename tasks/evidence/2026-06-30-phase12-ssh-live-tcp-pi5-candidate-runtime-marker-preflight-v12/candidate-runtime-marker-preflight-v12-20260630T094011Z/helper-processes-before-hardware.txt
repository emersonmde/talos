1945402 /bin/bash -lc set -Eeuo pipefail . "$HOME/.cargo/env" export TMPDIR=/opt/strider/openclaw/current/workspace/tmp export PATH=/opt/strider/openclaw/current/workspace/tools/qemu-9.2.0-install/bin:$PATH  API_BASE=$(printenv TALOS_LAB_API || true) if [ -z "$API_BASE" ]; then   API_BASE=http://talos-lab-api:8080 fi STATE=/opt/strider/openclaw/current/workspace/memory/talos-supervisor-state.json TASK_ID=phase12-ssh-live-tcp-pi5-candidate-runtime-marker-preflight-v12-20260630 RUN_TS=$(date -u +%Y%m%dT%H%M%SZ) RUN_LABEL=candidate-runtime-marker-preflight-v12-$RUN_TS EVIDENCE_ROOT=tasks/evidence/2026-06-30-phase12-ssh-live-tcp-pi5-candidate-runtime-marker-preflight-v12 EVIDENCE_DIR=$EVIDENCE_ROOT/$RUN_LABEL VALIDATION_DIR=$EVIDENCE_DIR/validation IDENTITY_DIR=$EVIDENCE_DIR/candidate-identity RUN_DIR=$EVIDENCE_DIR/candidate-run BOOT_SOURCE=target/tmp/rpi5-observed-gpio-status-known-good-tree BOOT_TREE=target/tmp/$RUN_LABEL-boot-tree ARCHIVE=target/tmp/$RUN_LABEL.tar.gz RESTORE_SNAPSHOT=phase12-ssh-v10-openssh-clean-pre-20260624T074100Z RESTORE_TREE=a0452458391d0e398b7e17e0f068bb652235f666bf277d004e0e214626128d10 NONCE=$RUN_LABEL ROUTE_START_MARKER="TALOS: ssh-service-smoltcp-runtime-route-start capture-nonce=$NONCE" RUNTIME_READY_MARKER="TALOS: ssh-service-smoltcp-runtime-ready capture-nonce=$NONCE" SERIAL_MARKER="$RUNTIME_READY_MARKER" LOCK_ACQUIRED=0  mkdir -p "$VALIDATION_DIR" "$IDENTITY_DIR" "$RUN_DIR"  update_lock_state() {   locked="$1"   restored="$2"   note="$3"   ts="$(date -u +%Y-%m-%dT%H:%M:%SZ)"   tmp="/opt/strider/openclaw/current/workspace/tmp/talos-state-lock-v12.$$.json"   jq --arg ts "$ts" --arg task "$TASK_ID" --arg note "$note" --argjson locked "$locked" --argjson restored "$restored" '     .hardwareTestLock.locked = $locked     | .hardwareTestLock.owner = (if $locked then "worker" else null end)     | .hardwareTestLock.taskId = $task     | (if $locked then .hardwareTestLock.acquiredAt = $ts | .hardwareTestLock.releasedAt = null else .hardwareTestLock.releasedAt = $ts end)     | .hardwareTestLock.restored = $restored     | .hardwareTestLock.note = $note     | .currentRunNote = $note   ' "$STATE" > "$tmp"   jq empty "$tmp"   mv "$tmp" "$STATE" }  cleanup() {   rc=$?   set +e   if [ "$LOCK_ACQUIRED" -eq 1 ]; then     current_tree="$(curl -fsS "$API_BASE/status" 2>/dev/null | jq -r '.boot.tree_hash // ""' 2>/dev/null)"     if [ "$current_tree" != "$RESTORE_TREE" ]; then       mkdir -p "$EVIDENCE_DIR"       curl -fsS -X POST "$API_BASE/boot/restore?name=$RESTORE_SNAPSHOT" > "$EVIDENCE_DIR/emergency-restore-snapshot.json" 2>"$EVIDENCE_DIR/emergency-restore.stderr.txt"       curl -fsS "$API_BASE/status" > "$EVIDENCE_DIR/emergency-post-restore-status.json" 2>/dev/null       current_tree="$(jq -r '.boot.tree_hash // ""' "$EVIDENCE_DIR/emergency-post-restore-status.json" 2>/dev/null)"     fi     if [ "$current_tree" = "$RESTORE_TREE" ]; then       update_lock_state false true "Worker released hardwareTestLock for $TASK_ID after restore to $RESTORE_SNAPSHOT."     else       update_lock_state false false "Worker released hardwareTestLock for $TASK_ID but restore proof is incomplete; inspect $EVIDENCE_DIR."     fi   fi   rm -f "$ARCHIVE"   rm -rf "$BOOT_TREE"   exit "$rc" } trap cleanup EXIT INT TERM  git status --short --branch > "$VALIDATION_DIR/git-status-before-action.txt" jq -e '.hardwareTestLock.locked == false and .hardwareTestLock.restored == true and .supervisorIntervention.active == false and .currentTask.id == "'"$TASK_ID"'" and .currentTask.status == "in_progress"' "$STATE" > "$VALIDATION_DIR/state-pre-lock-check.json" update_lock_state true false "Worker acquired hardwareTestLock for $TASK_ID repaired TFTP/runtime-marker preflight." LOCK_ACQUIRED=1 cp "$STATE" "$EVIDENCE_DIR/hardware-lock-state-before-lab.json"  printf '%s\n' "$BOOT_SOURCE" > "$IDENTITY_DIR/boot-source-dir.txt" printf '%s\n' "$RESTORE_SNAPSHOT" > "$IDENTITY_DIR/restore-snapshot-name.txt" printf '%s\n' "$NONCE" > "$IDENTITY_DIR/nonce.txt" printf '%s\n%s\n' "$ROUTE_START_MARKER" "$RUNTIME_READY_MARKER" > "$IDENTITY_DIR/required-readiness-markers.txt"  curl -fsS "$API_BASE/status" > "$EVIDENCE_DIR/baseline-status-before-candidate-action.json" curl -fsS "$API_BASE/boot/files" > "$EVIDENCE_DIR/baseline-boot-files-before-candidate-action.json" curl -fsS "$API_BASE/boot/snapshots" > "$EVIDENCE_DIR/baseline-boot-snapshots-before-candidate-action.json" jq -e --arg name "$RESTORE_SNAPSHOT" '(.snapshots // .boot.snapshots // []) | any(.name == $name or . == $name)' "$EVIDENCE_DIR/baseline-boot-snapshots-before-candidate-action.json" > "$VALIDATION_DIR/restore-snapshot-present.json"  if pgrep -af "rpi5-capture-invariant-proof-bundle|rpi5-observe-serial-window|rpi5-wait-tftp-delta|rpi5-retain-runtime-readiness-primary|rpi5-observe-runtime-readiness" > "$EVIDENCE_DIR/helper-processes-before-hardware.txt"; then   true else   printf 'no matching helper processes before hardware\n' > "$EVIDENCE_DIR/helper-processes-before-hardware.txt" fi  git rev-parse HEAD > "$IDENTITY_DIR/source-commit.txt" {   printf 'tftp-capture-boundary-reconciliation-v12 '   git merge-base --is-ancestor 1a199ad3f5f8416af2d5088214c5a6d3bf433433 HEAD && printf 'ancestor\n' || printf 'not-ancestor\n'   printf 'candidate-runtime-marker-route-repair '   git merge-base --is-ancestor 55c6920fbe86b233ea82b7d764e18f595b1e50cb HEAD && printf 'ancestor\n' || printf 'not-ancestor\n' } > "$IDENTITY_DIR/accepted-ancestry.txt"  rm -rf "$BOOT_TREE" set +e TALOS_CAPTURE_NONCE="$NONCE" ./scripts/rpi5-ssh-service-smoltcp-runtime-ready-boot-tree.sh "$BOOT_SOURCE" "$BOOT_TREE" > "$VALIDATION_DIR/rpi5-boot-tree.stdout.txt" 2> "$VALIDATION_DIR/rpi5-boot-tree.stderr.txt" boot_tree_exit=$? set -e printf '%s\n' "$boot_tree_exit" > "$VALIDATION_DIR/rpi5-boot-tree.exit" if [ "$boot_tree_exit" -ne 0 ]; then   exit "$boot_tree_exit" fi find "$BOOT_TREE" -type f -printf '%P\n' | sort > "$IDENTITY_DIR/boot-tree-manifest.txt" find "$BOOT_TREE" -type f -print0 | sort -z | xargs -0 sha256sum > "$IDENTITY_DIR/boot-tree-file-sha256.txt"  tar -C "$BOOT_TREE" -czf "$ARCHIVE" . set +e TALOS_CAPTURE_NONCE="$NONCE" ./scripts/rpi5-ssh-service-smoltcp-runtime-ready-archive-review.sh "$ARCHIVE" "$NONCE" > "$VALIDATION_DIR/rpi5-archive-review.stdout.txt" 2> "$VALIDATION_DIR/rpi5-archive-review.stderr.txt" archive_review_exit=$? set -e printf '%s\n' "$archive_review_exit" > "$VALIDATION_DIR/rpi5-archive-review.exit" if [ "$archive_review_exit" -ne 0 ]; then   exit "$archive_review_exit" fi  strings "$BOOT_TREE/kernel_2712.img" | rg "ssh-service-smoltcp-runtime-(route-start|ready)|capture-nonce=$NONCE|TALOS: kernel_main" > "$IDENTITY_DIR/candidate-token-review.txt" || true kernel_bytes=$(wc -c < "$BOOT_TREE/kernel_2712.img" | tr -d ' ') kernel_sha=$(sha256sum "$BOOT_TREE/kernel_2712.img" | awk '{print $1}') selected_kernel_bytes=$(wc -c < "$BOOT_TREE/da591740/kernel_2712.img" | tr -d ' ') selected_kernel_sha=$(sha256sum "$BOOT_TREE/da591740/kernel_2712.img" | awk '{print $1}') archive_sha=$(sha256sum "$ARCHIVE" | awk '{print $1}') archive_bytes=$(wc -c < "$ARCHIVE" | tr -d ' ') jq -n \   --arg task_id "$TASK_ID" \   --arg run_label "$RUN_LABEL" \   --arg source_commit "$(cat "$IDENTITY_DIR/source-commit.txt")" \   --arg archive_path_label "$ARCHIVE" \   --arg archive_sha "$archive_sha" \   --argjson archive_bytes "$archive_bytes" \   --arg expected_fetch "da591740/kernel_2712.img" \   --argjson expected_bytes "$selected_kernel_bytes" \   --arg kernel_sha "$selected_kernel_sha" \   --argjson root_kernel_bytes "$kernel_bytes" \   --arg root_kernel_sha "$kernel_sha" \   --arg route_start_marker "$ROUTE_START_MARKER" \   --arg runtime_ready_marker "$RUNTIME_READY_MARKER" \   '{task_id: $task_id, run_label: $run_label, source_commit: $source_commit,     archive: {path_label: $archive_path_label, sha256: $archive_sha, bytes: $archive_bytes},     expected_fetch: {path: $expected_fetch, bytes: $expected_bytes, kernel_sha256: $kernel_sha},     root_kernel: {path: "kernel_2712.img", bytes: $root_kernel_bytes, kernel_sha256: $root_kernel_sha},     marker_contract: {required_markers: [$route_start_marker, $runtime_ready_marker], require_kernel_marker: false},     runtime_marker_route_review: "validation/rpi5-archive-review.stdout.txt",     boot_artifact_bytes_retained: false}' > "$IDENTITY_DIR/archive-metadata.json"  curl -fsS -X PUT --data-binary @"$ARCHIVE" "$API_BASE/boot/archive" > "$IDENTITY_DIR/publish-candidate.json" curl -fsS "$API_BASE/status" > "$IDENTITY_DIR/status-after-publish-candidate.json" curl -fsS "$API_BASE/boot/files" > "$IDENTITY_DIR/boot-files-after-publish-candidate.json" curl -fsS "$API_BASE/boot/snapshots" > "$IDENTITY_DIR/boot-snapshots-after-publish-candidate.json"  expected_tree=$(jq -r '.boot.tree_hash // ""' "$IDENTITY_DIR/status-after-publish-candidate.json") expected_bytes=$(jq -r --arg f "da591740/kernel_2712.img" '(.boot.files // []) | map(select(.name == $f)) | first | .bytes // empty' "$IDENTITY_DIR/boot-files-after-publish-candidate.json") printf '%s\n' "$expected_tree" > "$RUN_DIR/expected-tree-hash.txt" printf '%s\n' "$expected_bytes" > "$RUN_DIR/expected-fetch-bytes.txt"  set +e ./scripts/rpi5-capture-invariant-proof-bundle.sh \   --evidence-dir "$RUN_DIR" \   --restore-snapshot "$RESTORE_SNAPSHOT" \   --label "$RUN_LABEL" \   --expected-tree-hash "$expected_tree" \   --expected-kernel kernel_2712.img \   --expected-fetch da591740/kernel_2712.img \   --expected-fetch-bytes "$expected_bytes" \   --serial-marker "$SERIAL_MARKER" \   --serial-timeout 120 \   --settle-ms 1000 \   --max-bytes 131072 \   --tftp-timeout 120 \   --stable-samples 3 \   > "$VALIDATION_DIR/capture-bundle.stdout.json" 2> "$VALIDATION_DIR/capture-bundle.stderr.txt" capture_exit=$? set -e printf '%s\n' "$capture_exit" > "$VALIDATION_DIR/capture-bundle.exit"  curl -fsS "$API_BASE/status" > "$EVIDENCE_DIR/status-after-helper.json" curl -fsS "$API_BASE/boot/files" > "$EVIDENCE_DIR/boot-files-after-helper.json" curl -fsS "$API_BASE/boot/snapshots" > "$EVIDENCE_DIR/boot-snapshots-after-helper.json"  post_helper_tree=$(jq -r '.boot.tree_hash // ""' "$EVIDENCE_DIR/status-after-helper.json") if [ "$post_helper_tree" != "$RESTORE_TREE" ]; then   curl -fsS -X POST "$API_BASE/boot/restore?name=$RESTORE_SNAPSHOT" > "$EVIDENCE_DIR/manual-restore-after-helper.json"   curl -fsS "$API_BASE/status" > "$EVIDENCE_DIR/status-after-manual-restore.json"   curl -fsS "$API_BASE/boot/files" > "$EVIDENCE_DIR/boot-files-after-manual-restore.json" else   jq -n --arg snapshot "$RESTORE_SNAPSHOT" --arg tree "$post_helper_tree" '{manual_restore_performed:false, restore_snapshot:$snapshot, post_helper_tree_hash:$tree}' > "$EVIDENCE_DIR/manual-restore-after-helper.json"   cp "$EVIDENCE_DIR/status-after-helper.json" "$EVIDENCE_DIR/status-after-manual-restore.json"   cp "$EVIDENCE_DIR/boot-files-after-helper.json" "$EVIDENCE_DIR/boot-files-after-manual-restore.json" fi  set +e ./scripts/rpi5-candidate-capture-window-v5-check.sh \   --evidence-dir "$RUN_DIR" \   --label "$RUN_LABEL" \   --report-kind candidate \   --nonce "$NONCE" \   > "$RUN_DIR/capture-window-v5-check.json" 2> "$VALIDATION_DIR/capture-window-v5-check.stderr.txt" check_exit=$? set -e printf '%s\n' "$check_exit" > "$VALIDATION_DIR/capture-window-v5-check.exit"  jq -n \   --arg route_start_marker "$ROUTE_START_MARKER" \   --arg runtime_ready_marker "$RUNTIME_READY_MARKER" \   --slurpfile serial "$RUN_DIR/serial-observe-window.json" \   '($serial[0].text // "") as $text    | {       checker: "retained-serial-required-marker-pair",       required_markers: [$route_start_marker, $runtime_ready_marker],       route_start_present: ($text | contains($route_start_marker)),       runtime_ready_present: ($text | contains($runtime_ready_marker)),       route_start_occurrences: (($text | split($route_start_marker) | length) - 1),       runtime_ready_occurrences: (($text | split($runtime_ready_marker) | length) - 1),       marker_pair_present: (($text | contains($route_start_marker)) and ($text | contains($runtime_ready_marker)))     }' > "$RUN_DIR/route-start-runtime-ready-marker-check.json"  jq -n \   --arg task_id "$TASK_ID" \   --arg run_label "$RUN_LABEL" \   --arg source_commit "$(cat "$IDENTITY_DIR/source-commit.txt")" \   --slurpfile archive "$IDENTITY_DIR/archive-metadata.json" \   --slurpfile baseline "$EVIDENCE_DIR/baseline-status-before-candidate-action.json" \   --slurpfile publish "$IDENTITY_DIR/status-after-publish-candidate.json" \   --slurpfile capture "$RUN_DIR/capture-invariant-summary.json" \   --slurpfile check "$RUN_DIR/capture-window-v5-check.json" \   --slurpfile markers "$RUN_DIR/route-start-runtime-ready-marker-check.json" \   --slurpfile post_helper "$EVIDENCE_DIR/status-after-helper.json" \   --slurpfile post_restore "$EVIDENCE_DIR/status-after-manual-restore.json" \   --argjson capture_exit "$capture_exit" \   --argjson check_exit "$check_exit" \   '{     task_id: $task_id,     run_label: $run_label,     source_commit: $source_commit,     archive: $archive[0],     baseline_tree_hash: ($baseline[0].boot.tree_hash // null),     post_publication_tree_hash: ($publish[0].boot.tree_hash // null),     capture_exit_code: $capture_exit,     capture_suggested_classification: ($capture[0].suggested_classification // null),     capture_checker_exit_code: $check_exit,     capture_checker_classification: ($check[0].classification // null),     checker_rejection_reasons: ($check[0].rejection_reasons // []),     marker_pair_check: $markers[0],     marker_visible: (($check[0].serial_capture.required_marker_occurrences // 0) > 0),     marker_nonce: ($check[0].serial_capture.nonce_token // null),     tftp_event_count: ($capture[0].tftp_event_count // null),     expected_fetch_count: ($capture[0].expected_fetch_count // null),     expected_fetch_byte_match_count: ($capture[0].expected_fetch_byte_match_count // null),     final_pre_restore_tree_hash: ($capture[0].final_pre_restore_identity.tree_hash // null),     post_helper_tree_hash: ($post_helper[0].boot.tree_hash // null),     post_restore_tree_hash: ($post_restore[0].boot.tree_hash // null),     restored_to_selected_control: (($post_restore[0].boot.tree_hash // null) == "a0452458391d0e398b7e17e0f068bb652235f666bf277d004e0e214626128d10")   }' > "$EVIDENCE_DIR/preflight-summary.json"  update_lock_state false true "Worker released hardwareTestLock for $TASK_ID after repaired v12 preflight restored to $RESTORE_SNAPSHOT." LOCK_ACQUIRED=0 rm -f "$ARCHIVE" rm -rf "$BOOT_TREE"  printf 'run_label=%s\n' "$RUN_LABEL" printf 'evidence_dir=%s\n' "$EVIDENCE_DIR" printf 'capture_exit=%s\n' "$capture_exit" printf 'capture_window_check_exit=%s\n' "$check_exit" jq -r '"suggested="+(.capture_suggested_classification // "null")+" checker="+(.capture_checker_classification // "null")+" marker_pair="+((.marker_pair_check.marker_pair_present // false)|tostring)+" expected_fetch_count="+((.expected_fetch_count // null)|tostring)+" tftp_events="+((.tftp_event_count // null)|tostring)+" rejections="+((.checker_rejection_reasons // [])|join(","))' "$EVIDENCE_DIR/preflight-summary.json"
