# V78 Selected-Kernel Entry Retention Comparison

V71 proved selected TFTP service plus retained exceptions-ready loop output for
the v70 archive. V76/V77 served the provider route-entry archive and retained
final selected identity but did not retain kernel_main or provider route/runtime
markers. V78 keeps the provider route feature path and adds a retained terminal
hierarchy after the provider report so v79 can classify the boundary without
relying on one-shot serial output.

The first missing hardware fact remains selected Pi 5 serial retention of
kernel_main after selected kernel TFTP service until v79 runs.
