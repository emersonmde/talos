1826143     110 Ss         00:00 /bin/bash -lc set -euo pipefail . "$HOME/.cargo/env" export TMPDIR=/opt/strider/openclaw/current/workspace/tmp export PATH=/opt/strider/openclaw/current/workspace/tools/qemu-9.2.0-install/bin:$PATH API=$(printenv TALOS_LAB_API || true) if [ -z "$API" ]; then API=http://talos-lab-api:8080; fi TASK=phase12-ssh-live-tcp-pi5-candidate-preflight-v6-20260629 STAMP=$(date -u +%Y%m%dT%H%M%SZ) RUN=candidate-preflight-v6-$STAMP BASE=tasks/evidence/2026-06-29-phase12-ssh-live-tcp-pi5-candidate-preflight-v6 E="$BASE/$RUN" VAL="$E/validation" IDENT="$E/candidate-identity" RUNDIR="$E/candidate-run" SRC=target/tmp/rpi5-observed-gpio-status-known-good-tree BOOT_TREE=target/tmp/$RUN-boot-tree ARCH=target/tmp/$RUN.tar.gz SNAP=phase12-ssh-v10-openssh-clean-pre-20260624T074100Z NONCE=$RUN MARKER="TALOS: ssh-service-smoltcp-runtime-ready capture-nonce=$NONCE" mkdir -p "$VAL" "$IDENT" "$RUNDIR" "$BASE"  git status --short --branch > "$VAL/git-status-before.txt" git rev-parse HEAD > "$IDENT/source-commit.txt" {   git merge-base --is-ancestor 74a1433cfa1312df7c6f96839c825095b28cff26 HEAD && printf '74a1433cfa1312df7c6f96839c825095b28cff26 ancestor=true\n'   git merge-base --is-ancestor e91159723769ceeb187b4dd10fc4f03deb0cb3d5 HEAD && printf 'e91159723769ceeb187b4dd10fc4f03deb0cb3d5 ancestor=true\n'   git merge-base --is-ancestor 5999653e6835bed996bd1dbb666c29609ab411d3 HEAD && printf '5999653e6835bed996bd1dbb666c29609ab411d3 ancestor=true\n'   git merge-base --is-ancestor b3ed0fe1af34cb5c037cde43c05ec76f143fad55 HEAD && printf 'b3ed0fe1af34cb5c037cde43c05ec76f143fad55 ancestor=true\n' } > "$IDENT/accepted-ancestry.txt"  curl -fsS "$API/status" > "$E/baseline-status-before-candidate-action.json" curl -fsS "$API/boot/files" > "$E/baseline-boot-files-before-candidate-action.json" curl -fsS "$API/boot/snapshots" > "$E/baseline-boot-snapshots-before-candidate-action.json" printf '%s\n' "$SNAP" > "$E/restore-snapshot-name.txt" printf '%s\n' "$SRC" > "$E/boot-source-dir.txt" printf '%s\n' "$MARKER" > "$RUNDIR/serial-marker.txt" printf '%s\n' "$NONCE" > "$RUNDIR/nonce.txt" ps -eo pid,ppid,stat,etime,cmd | rg 'rpi5-capture|rpi5-observe|rpi5-wait|boot/archive|power/cycle|serial/read|cargo|qemu' > "$E/no-helper-processes-before-hardware.txt" || true  export TALOS_CAPTURE_NONCE="$NONCE" set +e ./scripts/rpi5-boot-tree.sh "$SRC" "$BOOT_TREE" > "$VAL/rpi5-boot-tree.stdout.txt" 2> "$VAL/rpi5-boot-tree.stderr.txt" BOOT_EXIT=$? set -e printf '%s\n' "$BOOT_EXIT" > "$VAL/rpi5-boot-tree.exit" if [ "$BOOT_EXIT" -ne 0 ]; then   jq -n --arg task "$TASK" --arg run "$RUN" --arg evidence "$E" --arg classification "blocked-candidate-identity" \     '{task_id:$task, run_label:$run, evidence_dir:$evidence, classification:$classification, terminal:true, reason:"candidate archive materialization failed"}' > "$E/classification.json"   exit 0 fi mkdir -p "$BOOT_TREE/da591740" for file in config.txt cmdline.txt bcm2712-rpi-5-b.dtb kernel_2712.img kernel8.img; do   cp "$BOOT_TREE/$file" "$BOOT_TREE/da591740/$file" done if [ -d "$BOOT_TREE/overlays" ]; then   mkdir -p "$BOOT_TREE/da591740/overlays"   find "$BOOT_TREE/overlays" -maxdepth 1 -type f -exec cp {} "$BOOT_TREE/da591740/overlays/" \; fi find "$BOOT_TREE" -type f | sort > "$IDENT/boot-tree-manifest.txt" sha256sum "$BOOT_TREE"/kernel_2712.img "$BOOT_TREE"/da591740/kernel_2712.img > "$IDENT/boot-tree-file-sha256.txt" tar -czf "$ARCH" -C "$BOOT_TREE" . set +e ./scripts/rpi5-archive-review.sh "$ARCH" > "$VAL/rpi5-archive-review.stdout.txt" 2> "$VAL/rpi5-archive-review.stderr.txt" ARCHIVE_REVIEW_EXIT=$? set -e printf '%s\n' "$ARCHIVE_REVIEW_EXIT" > "$VAL/rpi5-archive-review.exit" if [ "$ARCHIVE_REVIEW_EXIT" -ne 0 ]; then   jq -n --arg task "$TASK" --arg run "$RUN" --arg evidence "$E" --arg classification "blocked-candidate-identity" \     '{task_id:$task, run_label:$run, evidence_dir:$evidence, classification:$classification, terminal:true, reason:"candidate archive review failed"}' > "$E/classification.json"   rm -f "$ARCH"   exit 0 fi ARCH_SHA=$(sha256sum "$ARCH" | awk '{print $1}') ARCH_BYTES=$(wc -c < "$ARCH" | tr -d ' ') KERNEL_SHA=$(sha256sum "$BOOT_TREE/da591740/kernel_2712.img" | awk '{print $1}') KERNEL_BYTES=$(wc -c < "$BOOT_TREE/da591740/kernel_2712.img" | tr -d ' ') jq -n \   --arg task "$TASK" \   --arg run "$RUN" \   --arg source_commit "$(cat "$IDENT/source-commit.txt")" \   --arg path_label "$ARCH" \   --arg archive_sha "$ARCH_SHA" \   --arg kernel_sha "$KERNEL_SHA" \   --argjson archive_bytes "$ARCH_BYTES" \   --argjson kernel_bytes "$KERNEL_BYTES" \   '{task_id:$task, run_label:$run, source_commit:$source_commit,     archive:{path_label:$path_label, sha256:$archive_sha, bytes:$archive_bytes},     expected_fetch:{path:"da591740/kernel_2712.img", bytes:$kernel_bytes, kernel_sha256:$kernel_sha},     boot_artifact_bytes_retained:false}' > "$IDENT/archive-metadata.json"  curl -fsS -X PUT --data-binary @"$ARCH" "$API/boot/archive" > "$IDENT/publish-candidate.json" curl -fsS "$API/status" > "$IDENT/status-after-publish-candidate.json" curl -fsS "$API/boot/files" > "$IDENT/boot-files-after-publish-candidate.json" curl -fsS "$API/boot/snapshots" > "$IDENT/boot-snapshots-after-publish-candidate.json" TREE=$(jq -r '.boot.tree_hash' "$IDENT/status-after-publish-candidate.json") BYTES=$(jq -r '.boot.files[] | select(.name=="da591740/kernel_2712.img") | .bytes' "$IDENT/boot-files-after-publish-candidate.json") printf '%s\n' "$TREE" > "$RUNDIR/expected-tree-hash.txt" printf '%s\n' "$BYTES" > "$RUNDIR/expected-fetch-bytes.txt"  set +e ./scripts/rpi5-capture-invariant-proof-bundle.sh \   --evidence-dir "$RUNDIR" \   --restore-snapshot "$SNAP" \   --label "$RUN" \   --expected-tree-hash "$TREE" \   --expected-kernel kernel_2712.img \   --expected-fetch da591740/kernel_2712.img \   --expected-fetch-bytes "$BYTES" \   --serial-marker "$MARKER" \   --serial-timeout 90 \   --tftp-timeout 90 \   --max-bytes 131072 > "$VAL/capture-bundle.stdout.json" 2> "$VAL/capture-bundle.stderr.txt" CAP_EXIT=$? set -e printf '%s\n' "$CAP_EXIT" > "$VAL/capture-bundle.exit"  if [ ! -f "$RUNDIR/post-restore-status.json" ]; then   curl -fsS -X POST "$API/boot/restore?name=$SNAP" > "$RUNDIR/emergency-restore-after-helper-exit.json" || true fi curl -fsS "$API/status" > "$E/status-after-helper.json" curl -fsS "$API/boot/files" > "$E/boot-files-after-helper.json" curl -fsS "$API/boot/snapshots" > "$E/boot-snapshots-after-helper.json"  set +e ./scripts/rpi5-candidate-capture-window-v5-check.sh \   --evidence-dir "$RUNDIR" \   --label "$RUN" \   --report-kind candidate \   --nonce "$NONCE" > "$RUNDIR/capture-window-v5-check.json" 2> "$VAL/capture-window-v5-check.stderr.txt" CHECK_EXIT=$? set -e printf '%s\n' "$CHECK_EXIT" > "$VAL/capture-window-v5-check.exit"  CLASSIFICATION=$(jq -r '   (.rejection_reasons // []) as $r |   if .decisive_rp1_hardware_classification_allowed == true then "candidate-capture-ready"   elif ($r | any(test("restore"))) then "blocked-restore"   elif ($r | any(test("capture-window|helper"))) then "blocked-capture-window-contract"   elif ($r | any(test("preflight|selected-tree|effective-kernel|expected-fetch-byte|candidate-identity"))) then "blocked-candidate-identity"   elif ($r | any(test("tftp|expected-fetch-not-observed"))) then "blocked-candidate-tftp-capture"   elif ($r | any(test("serial|marker|kernel"))) then "blocked-candidate-kernel-not-starting"   else "inconclusive-with-required-discriminator" end ' "$RUNDIR/capture-window-v5-check.json") SELECTED_NEXT=$(if [ "$CLASSIFICATION" = candidate-capture-ready ]; then printf '%s' phase12-ssh-live-tcp-pi5-packet-io-discriminator-20260629; else printf 'null'; fi) PLANNING=$(if [ "$CLASSIFICATION" = candidate-capture-ready ]; then printf 'false'; else printf 'true'; fi)  jq -n \   --arg task "$TASK" \   --arg run "$RUN" \   --arg evidence_dir "$E" \   --arg classification "$CLASSIFICATION" \   --arg selected_next "$SELECTED_NEXT" \   --argjson planning "$PLANNING" \   --argjson capture_exit "$CAP_EXIT" \   --argjson check_exit "$CHECK_EXIT" \   --slurpfile archive "$IDENT/archive-metadata.json" \   --slurpfile publish "$IDENT/status-after-publish-candidate.json" \   --slurpfile capture "$RUNDIR/capture-invariant-summary.json" \   --slurpfile checker "$RUNDIR/capture-window-v5-check.json" \   --slurpfile final "$E/status-after-helper.json" \   '{     task_id:$task,     run_label:$run,     evidence_dir:$evidence_dir,     classification:$classification,     terminal:true,     selected_next_task:(if $selected_next == "null" then null else $selected_next end),     planningNeeded:$planning,     exits:{capture_bundle:$capture_exit, capture_window_v5_check:$check_exit},     source_archive:$archive[0],     post_publication:{       tree_hash:$publish[0].boot.tree_hash,       effective_kernel:$publish[0].boot.effective_kernel,       configured_kernel:$publish[0].boot.configured_kernel     },     candidate_capture:{       suggested_classification:($capture[0].suggested_classification // null),       expected_fetch_count:($capture[0].expected_fetch_count // null),       expected_fetch_byte_match_count:($capture[0].expected_fetch_byte_match_count // null),       tftp_event_count:($capture[0].tftp_event_count // null),       final_pre_restore_identity:($capture[0].final_pre_restore_identity // null),       restore:($capture[0].restore // null)     },     run_ownership:{       helper_run_completed:($checker[0].capture_window_contract.helper_run_completed // false),       completed_at:($checker[0].capture_window_contract.completed_at // null),       completion_event_count:($checker[0].capture_window_contract.completion_event_count // null),       stage_order:($checker[0].capture_window_contract.stage_order // []),       final_pre_restore_before_restore:($checker[0].capture_window_contract.final_pre_restore_before_restore // false),       tftp_delta_before_restore:($checker[0].capture_window_contract.tftp_delta_before_restore // false)     },     checker:{       classification:$checker[0].classification,       decisive_allowed:($checker[0].decisive_rp1_hardware_classification_allowed // false),       rejection_reasons:($checker[0].rejection_reasons // []),       proof_run_identity:($checker[0].proof_run_identity // {})     },     final_lab:{       tree_hash:$final[0].boot.tree_hash,       effective_kernel:$final[0].boot.effective_kernel,       kernel_2712_bytes:(($final[0].boot.files // []) | map(select(.name=="da591740/kernel_2712.img")) | first | .bytes)     },     rejected_claims:[       "packet-I/O discriminator until candidate-capture-ready",       "OpenSSH/generated-root retry",       "remote receipt",       "compatibility",       "service success",       "ssh-ready=true",       "broad shell work",       "phase transition"     ]   }' > "$E/classification.json"  jq -n \   --arg task "$TASK" \   --arg run "$RUN" \   --arg evidence_dir "$E" \   --slurpfile c "$E/classification.json" \   '{task_id:$task, run_label:$run, evidence_dir:$evidence_dir, classification:$c[0].classification,     artifacts:{       classification:"classification.json",       preflight_summary:"preflight-summary.json",       archive_metadata:"candidate-identity/archive-metadata.json",       candidate_capture_summary:"candidate-run/capture-invariant-summary.json",       capture_window_order:"candidate-run/capture-window-order.json",       capture_window_v5_check:"candidate-run/capture-window-v5-check.json",       post_restore_status:"status-after-helper.json",       post_restore_boot_files:"boot-files-after-helper.json",       validations:"validation/"     },     validation:{       boot_tree_exit:"validation/rpi5-boot-tree.exit",       archive_review_exit:"validation/rpi5-archive-review.exit",       capture_bundle_exit:"validation/capture-bundle.exit",       capture_window_v5_check_exit:"validation/capture-window-v5-check.exit"     },     redaction:"Raw serial/TFTP text, peer/address/mac/ip-like fields, and raw log line fields in JSON evidence are scrubbed after checker execution before commit.",     selected_next_task:$c[0].selected_next_task,     planningNeeded:$c[0].planningNeeded,     rejected_claims:$c[0].rejected_claims}' > "$E/evidence-map.json"  jq -n \   --arg task "$TASK" \   --arg run "$RUN" \   --arg source_commit "$(cat "$IDENT/source-commit.txt")" \   --slurpfile archive "$IDENT/archive-metadata.json" \   --slurpfile baseline "$E/baseline-status-before-candidate-action.json" \   --slurpfile publish "$IDENT/status-after-publish-candidate.json" \   --slurpfile c "$E/classification.json" \   '{task_id:$task, run_label:$run, source_commit:$source_commit,     archive:$archive[0],     baseline_tree_hash:$baseline[0].boot.tree_hash,     post_publication_tree_hash:$publish[0].boot.tree_hash,     terminal_classification:$c[0].classification,     selected_next_task:$c[0].selected_next_task,     planningNeeded:$c[0].planningNeeded}' > "$E/preflight-summary.json"  find "$E" -name '*.json' -type f -print0 | while IFS= read -r -d '' f; do   tmp="$f.tmp"   jq 'walk(if type == "object" then with_entries(         if (.key | test("(?i)(peer|client|address|addr|mac|ip|raw|line|text|payload|packet)"))         then .value = "[redacted]"         else .         end)       else . end)' "$f" > "$tmp" && mv "$tmp" "$f" done  find "$E" -name '*.json' -type f -print0 | xargs -0 -n1 jq empty  rm -f "$ARCH" jq -r '"classification=\(.classification) checker=\(.checker.classification) capture_exit=\(.exits.capture_bundle) check_exit=\(.exits.capture_window_v5_check) helper_completed=\(.run_ownership.helper_run_completed) selected_next=\(.selected_next_task // "null") final_tree=\(.final_lab.tree_hash)"' "$E/classification.json"
1826182 1826143 S          00:00 rg rpi5-capture|rpi5-observe|rpi5-wait|boot/archive|power/cycle|serial/read|cargo|qemu
