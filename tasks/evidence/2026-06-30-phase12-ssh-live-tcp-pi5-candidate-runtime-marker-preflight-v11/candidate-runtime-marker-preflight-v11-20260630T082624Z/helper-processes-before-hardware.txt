1936413 /bin/bash -lc  set -Eeuo pipefail . "$HOME/.cargo/env" export TMPDIR=/opt/strider/openclaw/current/workspace/tmp export PATH=/opt/strider/openclaw/current/workspace/tools/qemu-9.2.0-install/bin:$PATH  API_BASE=${TALOS_LAB_API:-http://talos-lab-api:8080} STATE=/opt/strider/openclaw/current/workspace/memory/talos-supervisor-state.json TASK_ID=phase12-ssh-live-tcp-pi5-candidate-runtime-marker-preflight-v11-20260630 RUN_TS=$(date -u +%Y%m%dT%H%M%SZ) RUN_LABEL=candidate-runtime-marker-preflight-v11-$RUN_TS EVIDENCE_ROOT=tasks/evidence/2026-06-30-phase12-ssh-live-tcp-pi5-candidate-runtime-marker-preflight-v11 EVIDENCE_DIR=$EVIDENCE_ROOT/$RUN_LABEL VALIDATION_DIR=$EVIDENCE_DIR/validation IDENTITY_DIR=$EVIDENCE_DIR/candidate-identity BOOT_SOURCE=target/tmp/rpi5-observed-gpio-status-known-good-tree BOOT_TREE=target/tmp/$RUN_LABEL-boot-tree ARCHIVE=target/tmp/$RUN_LABEL.tar.gz RESTORE_SNAPSHOT=phase12-ssh-v10-openssh-clean-pre-20260624T074100Z NONCE=$RUN_LABEL ROUTE_START_MARKER="TALOS: ssh-service-smoltcp-runtime-route-start capture-nonce=$NONCE" RUNTIME_READY_MARKER="TALOS: ssh-service-smoltcp-runtime-ready capture-nonce=$NONCE" REQUIRED_MARKERS="$ROUTE_START_MARKER|$RUNTIME_READY_MARKER" LOCK_ACQUIRED=0 RESTORE_DONE=0 RESTORE_OK=false  mkdir -p "$VALIDATION_DIR" "$IDENTITY_DIR"  update_lock_state() {   locked="$1"   restored="$2"   note="$3"   ts="$(date -u +%Y-%m-%dT%H:%M:%SZ)"   tmp="$STATE.tmp"   jq --arg ts "$ts" --arg task "$TASK_ID" --arg note "$note" --argjson locked "$locked" --argjson restored "$restored" '     .hardwareTestLock.locked = $locked     | .hardwareTestLock.owner = (if $locked then "worker" else null end)     | .hardwareTestLock.taskId = $task     | (if $locked then .hardwareTestLock.acquiredAt = $ts | .hardwareTestLock.releasedAt = null else .hardwareTestLock.releasedAt = $ts end)     | .hardwareTestLock.restored = $restored     | .hardwareTestLock.note = $note     | .currentRunNote = $note   ' "$STATE" > "$tmp"   jq empty "$tmp"   mv "$tmp" "$STATE" }  cleanup() {   rc=$?   if [ "$LOCK_ACQUIRED" -eq 1 ]; then     if [ "$RESTORE_DONE" -eq 0 ]; then       mkdir -p "$EVIDENCE_DIR"       if curl -fsS -X POST "$API_BASE/boot/restore?name=$RESTORE_SNAPSHOT" > "$EVIDENCE_DIR/emergency-restore-snapshot.json" 2>"$EVIDENCE_DIR/emergency-restore.stderr.txt"; then         RESTORE_DONE=1         RESTORE_OK=true         curl -fsS "$API_BASE/status" > "$EVIDENCE_DIR/emergency-post-restore-status.json" || true       else         RESTORE_OK=false       fi     fi     if [ "$RESTORE_OK" = true ]; then       update_lock_state false true "Worker released hardwareTestLock for $TASK_ID after restore to $RESTORE_SNAPSHOT."     else       update_lock_state false false "Worker released hardwareTestLock for $TASK_ID but restore proof is incomplete; inspect $EVIDENCE_DIR."     fi   fi   rm -f "$ARCHIVE"   exit "$rc" } trap cleanup EXIT INT TERM  git status --short --branch > "$VALIDATION_DIR/git-status-before-action.txt" printf '%s\n' "$BOOT_SOURCE" > "$IDENTITY_DIR/boot-source-dir.txt" printf '%s\n' "$RESTORE_SNAPSHOT" > "$IDENTITY_DIR/restore-snapshot-name.txt" printf '%s\n' "$NONCE" > "$IDENTITY_DIR/nonce.txt" printf '%s\n' "$REQUIRED_MARKERS" > "$IDENTITY_DIR/required-readiness-markers.txt"  jq -e '.hardwareTestLock.locked == false and .hardwareTestLock.restored == true and .supervisorIntervention.active == false and .currentTask.id == "'"$TASK_ID"'"' "$STATE" > "$VALIDATION_DIR/state-pre-lock-check.json" update_lock_state true false "Worker acquired hardwareTestLock for $TASK_ID v11 candidate runtime-marker preflight." LOCK_ACQUIRED=1 cp "$STATE" "$EVIDENCE_DIR/hardware-lock-state-before-lab.json"  curl -fsS "$API_BASE/status" > "$EVIDENCE_DIR/baseline-status-before-candidate-action.json" curl -fsS "$API_BASE/boot/files" > "$EVIDENCE_DIR/baseline-boot-files-before-candidate-action.json" curl -fsS "$API_BASE/boot/snapshots" > "$EVIDENCE_DIR/baseline-boot-snapshots-before-candidate-action.json" jq -e --arg name "$RESTORE_SNAPSHOT" '(.snapshots // .boot.snapshots // []) | any(.name == $name or . == $name)' "$EVIDENCE_DIR/baseline-boot-snapshots-before-candidate-action.json" > "$VALIDATION_DIR/restore-snapshot-present.json"  if pgrep -af "rpi5-capture-invariant-proof-bundle|rpi5-observe-serial-window|rpi5-wait-tftp-delta|rpi5-retain-runtime-readiness-primary|rpi5-observe-runtime-readiness" > "$EVIDENCE_DIR/helper-processes-before-hardware.txt"; then   true else   printf 'no matching helper processes before hardware\n' > "$EVIDENCE_DIR/helper-processes-before-hardware.txt" fi  git rev-parse HEAD > "$IDENTITY_DIR/source-commit.txt" git merge-base --is-ancestor 55c6920fbe86b233ea82b7d764e18f595b1e50cb HEAD && printf 'candidate-no-runtime-marker-reconciliation ancestor\n' > "$IDENTITY_DIR/accepted-ancestry.txt"  rm -rf "$BOOT_TREE" TALOS_CAPTURE_NONCE="$NONCE" ./scripts/rpi5-ssh-service-smoltcp-runtime-ready-boot-tree.sh "$BOOT_SOURCE" "$BOOT_TREE" > "$VALIDATION_DIR/rpi5-boot-tree.stdout.txt" 2> "$VALIDATION_DIR/rpi5-boot-tree.stderr.txt" find "$BOOT_TREE" -type f -printf '%P\n' | sort > "$IDENTITY_DIR/boot-tree-manifest.txt" find "$BOOT_TREE" -type f -print0 | sort -z | xargs -0 sha256sum > "$IDENTITY_DIR/boot-tree-file-sha256.txt"  tar -C "$BOOT_TREE" -czf "$ARCHIVE" . TALOS_CAPTURE_NONCE="$NONCE" ./scripts/rpi5-ssh-service-smoltcp-runtime-ready-archive-review.sh "$ARCHIVE" "$NONCE" > "$VALIDATION_DIR/rpi5-archive-review.stdout.txt" 2> "$VALIDATION_DIR/rpi5-archive-review.stderr.txt" strings "$BOOT_TREE/kernel_2712.img" | rg "ssh-service-smoltcp-runtime-(route-start|ready)|capture-nonce=$NONCE|TALOS: kernel_main" > "$IDENTITY_DIR/candidate-token-review.txt" || true  kernel_bytes=$(wc -c < "$BOOT_TREE/kernel_2712.img" | tr -d ' ') kernel_sha=$(sha256sum "$BOOT_TREE/kernel_2712.img" | awk '{print $1}') archive_sha=$(sha256sum "$ARCHIVE" | awk '{print $1}') archive_bytes=$(wc -c < "$ARCHIVE" | tr -d ' ') jq -n \   --arg task_id "$TASK_ID" \   --arg run_label "$RUN_LABEL" \   --arg source_commit "$(cat "$IDENTITY_DIR/source-commit.txt")" \   --arg archive_path_label "$ARCHIVE" \   --arg archive_sha "$archive_sha" \   --argjson archive_bytes "$archive_bytes" \   --arg expected_fetch "da591740/kernel_2712.img" \   --argjson expected_bytes "$kernel_bytes" \   --arg kernel_sha "$kernel_sha" \   --arg required_markers "$REQUIRED_MARKERS" \   '{task_id:$task_id, run_label:$run_label, source_commit:$source_commit,     archive:{path_label:$archive_path_label, sha256:$archive_sha, bytes:$archive_bytes},     expected_fetch:{path:$expected_fetch, bytes:$expected_bytes, kernel_sha256:$kernel_sha},     marker_contract:{required_markers:($required_markers|split("|")), require_kernel_marker:false},     generated_boot_bytes_retained:false}' > "$IDENTITY_DIR/archive-metadata.json"  curl -fsS -X PUT --data-binary @"$ARCHIVE" "$API_BASE/boot/archive" > "$IDENTITY_DIR/publish-candidate.json" curl -fsS "$API_BASE/status" > "$IDENTITY_DIR/status-after-publish-candidate.json" curl -fsS "$API_BASE/boot/files" > "$IDENTITY_DIR/boot-files-after-publish-candidate.json" curl -fsS "$API_BASE/boot/snapshots" > "$IDENTITY_DIR/boot-snapshots-after-publish-candidate.json"  expected_tree=$(jq -r '.boot.tree_hash // ""' "$IDENTITY_DIR/status-after-publish-candidate.json") expected_bytes=$(jq -r --arg f "da591740/kernel_2712.img" '(.boot.files // []) | map(select(.name == $f)) | first | .bytes // empty' "$IDENTITY_DIR/boot-files-after-publish-candidate.json") printf '%s\n' "$expected_tree" > "$EVIDENCE_DIR/expected-tree-hash.txt" printf '%s\n' "$expected_bytes" > "$EVIDENCE_DIR/expected-fetch-bytes.txt"  jq -n \   --arg expected_tree_hash "$expected_tree" \   --arg expected_kernel "kernel_2712.img" \   --arg expected_fetch "da591740/kernel_2712.img" \   --arg expected_fetch_bytes "$expected_bytes" \   --slurpfile status "$IDENTITY_DIR/status-after-publish-candidate.json" \   --slurpfile files "$IDENTITY_DIR/boot-files-after-publish-candidate.json" \   '(($expected_fetch_bytes|tonumber?)//null) as $expected_bytes_num    | (($files[0].boot.files // []) | map(select(.name == $expected_fetch)) | first) as $fetch    | {expected_tree_hash:$expected_tree_hash, observed_tree_hash:($status[0].boot.tree_hash // null),       expected_kernel:$expected_kernel, observed_effective_kernel:($status[0].boot.effective_kernel // null),       expected_fetch:$expected_fetch, expected_fetch_bytes:$expected_bytes_num,       observed_fetch_bytes:($fetch.bytes // null),       tree_matches:(($status[0].boot.tree_hash // null) == $expected_tree_hash),       effective_kernel_matches:(($status[0].boot.effective_kernel // null) == $expected_kernel),       expected_fetch_present:($fetch != null),       expected_fetch_bytes_match:(($fetch.bytes // null) == $expected_bytes_num),       staging_publication_mismatch:((($status[0].boot.tree_hash // null) != $expected_tree_hash) or (($status[0].boot.effective_kernel // null) != $expected_kernel) or ($fetch == null) or (($fetch.bytes // null) != $expected_bytes_num))}' > "$EVIDENCE_DIR/preflight-identity.json" jq -e '.staging_publication_mismatch == false' "$EVIDENCE_DIR/preflight-identity.json" > "$VALIDATION_DIR/preflight-identity-pass.json"  curl -fsS "${API_BASE}/serial/peek?max_bytes=65536&drain=true" > "$EVIDENCE_DIR/pre-power-serial-peek.json" serial_drain_tmp="$EVIDENCE_DIR/serial-drain-responses.jsonl" : > "$serial_drain_tmp" drain_attempts=0 drain_total_bytes=0 drain_empty=false drain_cursor=0 while [ "$drain_attempts" -lt 16 ]; do   payload="$(jq -n '{timeout_seconds:1, settle_ms:100, max_bytes:65536}')"   curl -fsS -X POST -H 'Content-Type: application/json' --data "$payload" "$API_BASE/serial/read" > "$EVIDENCE_DIR/serial-read-before-power-attempt-$((drain_attempts+1)).json"   cat "$EVIDENCE_DIR/serial-read-before-power-attempt-$((drain_attempts+1)).json" >> "$serial_drain_tmp"   printf '\n' >> "$serial_drain_tmp"   drain_attempts=$((drain_attempts + 1))   drain_bytes="$(jq -r '(.bytes // ((.text // "") | length))' "$EVIDENCE_DIR/serial-read-before-power-attempt-$drain_attempts.json")"   drain_cursor="$(jq -r '(.cursor // .cursor_end // 0)' "$EVIDENCE_DIR/serial-read-before-power-attempt-$drain_attempts.json")"   drain_total_bytes=$((drain_total_bytes + drain_bytes))   if [ "$drain_bytes" -eq 0 ]; then     drain_empty=true     break   fi done jq -s --argjson attempts "$drain_attempts" --argjson total_bytes "$drain_total_bytes" --argjson final_cursor "$drain_cursor" --argjson empty_before_power "$drain_empty" '{   action:"serial drain before power", responses:., talos_serial_drain:{contract_version:"pi5-capture-transaction-v2", attempts:$attempts, attempt_limit:16, read_timeout_seconds:1, settle_ms:100, max_bytes_per_read:65536, total_bytes:$total_bytes, final_cursor:$final_cursor, empty_before_power:$empty_before_power, discriminator:(if $empty_before_power then "empty-read-before-power" else "bounded-drain-exhausted-before-power" end)} }' "$serial_drain_tmp" > "$EVIDENCE_DIR/serial-drain-before-power.json"  curl -fsS "$API_BASE/tftp/logs?max_bytes=1048576&limit=1" > "$EVIDENCE_DIR/tftp-cursor-before-power.json" serial_cursor="$(jq -r '.talos_serial_drain.final_cursor' "$EVIDENCE_DIR/serial-drain-before-power.json")" tftp_cursor="$(jq -r '.tftp.cursor_end' "$EVIDENCE_DIR/tftp-cursor-before-power.json")" printf '%s\n' "$serial_cursor" > "$EVIDENCE_DIR/serial-cursor-before-power.txt" printf '%s\n' "$tftp_cursor" > "$EVIDENCE_DIR/tftp-cursor-before-power.txt"  curl -fsS -X POST "$API_BASE/power/cycle" > "$EVIDENCE_DIR/power-cycle.json"  set +e TALOS_READINESS_REQUIRED_MARKERS="$REQUIRED_MARKERS" TALOS_READINESS_REQUIRE_KERNEL_MARKER=false \   ./scripts/rpi5-retain-runtime-readiness-primary.sh "$EVIDENCE_DIR" "$RUN_LABEL" "$serial_cursor" 120 1000 131072 \   > "$VALIDATION_DIR/runtime-readiness-retention.stdout.txt" 2> "$VALIDATION_DIR/runtime-readiness-retention.stderr.txt" readiness_exit=$? set -e printf '%s\n' "$readiness_exit" > "$VALIDATION_DIR/runtime-readiness-retention.exit"  set +e ./scripts/rpi5-wait-tftp-delta.sh "$tftp_cursor" 120 3 > "$EVIDENCE_DIR/tftp-delta-stable-pre-restore.json" 2> "$VALIDATION_DIR/tftp-delta-stable-pre-restore.stderr.txt" tftp_exit=$? set -e printf '%s\n' "$tftp_exit" > "$VALIDATION_DIR/tftp-delta-stable-pre-restore.exit"  curl -fsS "$API_BASE/status" > "$EVIDENCE_DIR/final-pre-restore-status.json" curl -fsS "$API_BASE/boot/files" > "$EVIDENCE_DIR/final-pre-restore-boot-files.json"  curl -fsS -X POST "$API_BASE/boot/restore?name=$RESTORE_SNAPSHOT" > "$EVIDENCE_DIR/restore-snapshot.json" RESTORE_DONE=1 curl -fsS "$API_BASE/status" > "$EVIDENCE_DIR/post-restore-status.json" curl -fsS "$API_BASE/boot/files" > "$EVIDENCE_DIR/post-restore-boot-files.json" post_restore_tree=$(jq -r '.boot.tree_hash // ""' "$EVIDENCE_DIR/post-restore-status.json") if [ "$post_restore_tree" = "a0452458391d0e398b7e17e0f068bb652235f666bf277d004e0e214626128d10" ]; then   RESTORE_OK=true else   RESTORE_OK=false fi  jq -n \   --arg task_id "$TASK_ID" \   --arg run_label "$RUN_LABEL" \   --arg source_commit "$(cat "$IDENTITY_DIR/source-commit.txt")" \   --slurpfile archive "$IDENTITY_DIR/archive-metadata.json" \   --slurpfile preflight "$EVIDENCE_DIR/preflight-identity.json" \   --slurpfile readiness "$EVIDENCE_DIR/${RUN_LABEL}-runtime-readiness-primary-summary.json" \   --slurpfile tftp "$EVIDENCE_DIR/tftp-delta-stable-pre-restore.json" \   --slurpfile final_status "$EVIDENCE_DIR/final-pre-restore-status.json" \   --slurpfile final_files "$EVIDENCE_DIR/final-pre-restore-boot-files.json" \   --slurpfile restore "$EVIDENCE_DIR/restore-snapshot.json" \   --slurpfile post_restore "$EVIDENCE_DIR/post-restore-status.json" \   --argjson readiness_exit "$readiness_exit" \   --argjson tftp_exit "$tftp_exit" \   --arg expected_fetch "da591740/kernel_2712.img" \   --arg expected_tree "$expected_tree" \   --arg expected_bytes "$expected_bytes" \   --arg restore_snapshot "$RESTORE_SNAPSHOT" \   '($readiness[0].talos_runtime_readiness // {}) as $rr    | (($expected_bytes|tonumber?)//null) as $expected_bytes_num    | (($tftp[0].tftp.events // []) | map(select(.filename == $expected_fetch))) as $fetch_events    | ($fetch_events | map(select(.status == "served" and .bytes == $expected_bytes_num))) as $matching_fetch_events    | (($final_files[0].boot.files // []) | map(select(.name == $expected_fetch)) | first) as $final_fetch    | ($final_status[0].boot.tree_hash // null) as $final_tree    | ($post_restore[0].boot.tree_hash // null) as $post_restore_tree    | [       (if ($preflight[0].staging_publication_mismatch // true) then "candidate-identity" else empty end),       (if ($tftp[0].talos_tftp_stability.stable // false) != true then "candidate-tftp-capture" else empty end),       (if (($fetch_events|length) == 0) then "candidate-tftp-capture" else empty end),       (if (($fetch_events|length) > 0 and ($matching_fetch_events|length) != ($fetch_events|length)) then "candidate-identity" else empty end),       (if ($final_tree != $expected_tree) then "candidate-identity" else empty end),       (if ($final_fetch == null or ($final_fetch.bytes // null) != $expected_bytes_num) then "candidate-identity" else empty end),       (if ($rr.valid_known_good_talos_readiness == true) then empty else "runtime-marker-route" end),       (if ($post_restore_tree == null) then "restore" else empty end)     ] as $missing    | {       task_id:$task_id,       run_label:$run_label,       source_commit:$source_commit,       archive:$archive[0],       preflight_identity:$preflight[0],       readiness_exit_code:$readiness_exit,       runtime_readiness:$rr,       tftp_exit_code:$tftp_exit,       tftp_event_count:(($tftp[0].tftp.events // [])|length),       expected_fetch_count:($fetch_events|length),       expected_fetch_byte_match_count:($matching_fetch_events|length),       final_pre_restore_tree_hash:$final_tree,       post_restore_tree_hash:$post_restore_tree,       restore_snapshot:$restore_snapshot,       first_missing_fact:($missing[0] // null),       terminal_classification:(         if ($missing|length)==0 then "candidate-capture-ready"         elif ($missing[0]=="candidate-identity") then "blocked-candidate-identity"         elif ($missing[0]=="candidate-tftp-capture") then "blocked-candidate-tftp-capture"         elif ($missing[0]=="runtime-marker-route" and (($fetch_events|length)>0)) then "blocked-runtime-marker-route"         elif ($missing[0]=="runtime-marker-route") then "blocked-candidate-kernel-not-starting"         elif ($missing[0]=="restore") then "blocked-restore"         else "inconclusive-with-required-discriminator" end),       selected_next_task:(if ($missing|length)==0 then "phase12-ssh-live-tcp-pi5-packet-io-discriminator-20260629" else null end),       planningNeeded:(if ($missing|length)==0 then false else true end),       no_packet_io:true,       no_openssh_generated_root_retry:true     }' > "$EVIDENCE_DIR/candidate-preflight-analysis.json"  jq -n \   --arg task_id "$TASK_ID" \   --arg run_label "$RUN_LABEL" \   --arg run_dir "$EVIDENCE_DIR/" \   --slurpfile analysis "$EVIDENCE_DIR/candidate-preflight-analysis.json" \   '{     task_id:$task_id,     terminal_classification:$analysis[0].terminal_classification,     planningNeeded:$analysis[0].planningNeeded,     planningReason:(if $analysis[0].planningNeeded then ("v11 candidate first missing fact: " + ($analysis[0].first_missing_fact // "unknown")) else null end),     selected_next_task:$analysis[0].selected_next_task,     hardwareAction:true,     labPublication:true,     bootSnapshotMutation:true,     pi5PowerAction:true,     packetIo:false,     opensshGeneratedRootRetry:false,     run_label:$run_label,     evidence_paths:{       run_dir:$run_dir,       analysis:($run_dir + "candidate-preflight-analysis.json"),       archive_metadata:($run_dir + "candidate-identity/archive-metadata.json"),       publish_archive:($run_dir + "candidate-identity/publish-candidate.json"),       serial_primary:($run_dir + $run_label + "-runtime-readiness-primary.json"),       serial_primary_summary:($run_dir + $run_label + "-runtime-readiness-primary-summary.json"),       tftp_delta:($run_dir + "tftp-delta-stable-pre-restore.json"),       final_pre_restore_status:($run_dir + "final-pre-restore-status.json"),       restore_snapshot:($run_dir + "restore-snapshot.json"),       post_restore_status:($run_dir + "post-restore-status.json")     },     validation_results:{       git_status_before_action:"recorded",       candidate_archive_materialization:"pass",       candidate_archive_static_marker_selected_fetch_review:"pass",       lab_api_status_files_snapshots_before_after_restore:"pass",       fresh_serial_cursor_before_power:"pass",       tftp_cursor_and_stable_delta:(if ($analysis[0].expected_fetch_count // 0)>0 then "pass" else "blocked/no selected fetch in stable delta" end),       restore_confirmation:(if ($analysis[0].post_restore_tree_hash // "") == "a0452458391d0e398b7e17e0f068bb652235f666bf277d004e0e214626128d10" then "pass" else "blocked" end),       marker_checker:$analysis[0].terminal_classification     },     redaction_review:{       raw_serial_retained_as_task_owned_hardware_evidence:true,       raw_tftp_peer_logs_retained:true,       packet_payloads_retained:false,       key_or_session_material_retained:false,       boot_artifact_bytes_retained:false,       private_user_data_retained:false,       summary_json_omits_raw_serial_text_and_peer_fields:true     },     commit:null   }' > "$EVIDENCE_DIR/evidence-map.json"  rm -rf "$BOOT_TREE" rm -f "$ARCHIVE"  jq empty "$EVIDENCE_DIR"
