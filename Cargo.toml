[package]
name = "talos"
version = "0.1.0"
edition = "2024"
build = "build.rs"
license = "MIT"

[dependencies]
smoltcp = { version = "0.13.1", default-features = false, features = [
    "medium-ethernet",
    "proto-ipv4",
    "socket-tcp",
] }

[[bin]]
name = "talos"
test = true
bench = false
